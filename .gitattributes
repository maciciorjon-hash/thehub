# A TSV's last column is often empty, and an empty last column IS a trailing tab. Stripping it
# would change the column count, so whitespace checking does not apply to these files.
*.tsv -whitespace
