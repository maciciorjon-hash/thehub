# Generated — do not commit
dHUB.html
labbook-standalone.html
The Hub.html
dist/

# macOS
.DS_Store

# Large binary dependencies (Labmate uses CDN fallback)
Labmate/RDKit_minimal.js
Labmate/RDKit_minimal.wasm

# Local-only Labmate extras
Labmate/memory/
Labmate/Images/
Labmate/LabMate Protocols/
Labmate/*.command
Labmate/CLAUDE.md
Labmate/labmate backup.html

# Protocol/data files
*.txt
*.labcalc_known_protocols.txt

# Labbook daily backups (data, not source)
Labbook/backups/*.json

# OneNote import bundle (Jon's personal notebook data, never commit)
OneNoteImport/

# Claude internal
.claude/
.superpowers/

# Browser automation tooling (local testing only)
node_modules/
package.json
package-lock.json

# Your notebook/journal backup files — real lab data, never commit
Backup/
dhub-backup-*.json
labbook-*.json

# Finder/OS duplicates ("hub-shell 2.html", "echo copy.html"). Untracked anyway, but this
# stops one being committed by accident and stops anyone editing the wrong file thinking
# it's the source of truth.
* [0-9].html
*copy*.html
