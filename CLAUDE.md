# dHUB

> Auto-updated section at bottom. Static content below is maintained manually.

---

## Project overview

**dHUB** is the primary product (`dHUB.html`). It is a **self-contained** single-file launcher: all app HTMLs are base64-encoded and embedded directly inside it. Opening `dHUB.html` alone gives access to every tool, no other files required.

**Individual standalone files** also exist in their subfolders and are kept in sync — they serve as standalone versions of each app.

**Location:** `Desktop › The_Hub › dHUB.html`  
**Author:** Jon Macicior — postdoc, Ciulli Lab, University of Dundee  
**Stack:** Vanilla HTML/CSS/JS only. No build step, no server. Open in browser directly.

---

## Current apps

| ID | Name | Logo | Accent | Standalone file |
|----|------|------|--------|-----------------|
| `echo` | Echo (formerly Labcyte Echo / Echo Data Analysis) | SVG bar chart | `#ff5760` | `Echo/echo.html` |
| `lm` | LabMate (hidden from the home grid, not deleted) | SVG flask | `#e08c30` (amber) | `Labmate/labmate.html` |
| `deg` | Dora (formerly Degradation Explorer) | SVG curve | `#7c6fd4` | `Dora/dora.html` |
| `pd` | Blueprint (formerly Lab Designer) | SVG wells | `#0079b9` | `Blueprint/blueprint.html` |
| `dna` | Helix | SVG helix | `#43a047` | `Helix/helix.html` |
| `pt` | Protein Tools | SVG chain | `#9c6fd4` | `Protein_Tools/protein_tools.html` |
| `spectra` | BCA (formerly Spectra) | SVG standard curve | `#26a69a` | `BCA/bca.html` |
| `ldi` | LDI | SVG balance/scale | `#e91e63` | `LDI/ldi.html` |
| `cryo` | Iceberg | SVG snowflake | `#00acc1` | `Iceberg/iceberg.html` |
| `cuppa` | Cuppa | SVG coffee cup | `#8d6e63` | `Cuppa/cuppa.html` |
| `fabricata` | Fabricata™ | SVG bar chart + star | `#c07a8e` | `Fabricata/fabricata.html` |
| `beacon` | Beacon | SVG donor/acceptor BRET glyph | `#5e72c4` | `Beacon/beacon.html` |
| `lumina` | Lumina | SVG light bulb | `#f5c518` (warm gold) | `Lumina/lumina.html` |
| `ribbon` | Ribbon | SVG ribbon waves | `#e36c69` (salmon) | `Ribbon/ribbon.html` |
| `arc` | Arc (hidden from the home grid, not deleted) | SVG narrative arc + nodes | `#816baf` (purple) | `Arc/arc.html` |
| `protocols` | Archive (formerly Protocols) | SVG open book | `#a56983` (dusty pink) | `Archive/archive.html` |
| `cellarchive` | Cell Archive | SVG cell/nucleus | `#d17a4a` (terracotta) | `Cell_Archive/cell_archive.html` |
| `bench` | Bench (**admin-only** — Jon's private notebook) | SVG house/bench | `#4f9d8f` (teal) | `Bench/bench.html` |
| `labbook` | Labbook (**admin-only** — electronic lab notebook / OneNote replacement) | SVG notebook | `#4f9d8f` (teal) | `Labbook/labbook.html` |
| `plasmids` | Plasmids (**admin-only** — plasmid database + SnapGene maps) | SVG plasmid | `#6d7bd0` (indigo) | `Plasmids/plasmids.html` |

**Home grid packages:** the home cards are grouped into labelled sections by `PACKAGES` in `hub-shell.html` (Data Analysis · Design & Presentation · Molecular Biology · Lab Operations · Just for Fun). `_buildPackages()` reorganizes the flat `#app-grid` into per-package `.grid` blocks at load; drag-reorder is scoped within a package and persisted per-package in `localStorage` (`hub_card_order_v2`). Empty sections auto-hide via `_updateSectionVisibility()`. Any app id not in `PACKAGES` falls into a trailing "More" section.

---

## Architecture & workflow

**dHUB is self-contained.** Each app's HTML is base64-encoded and stored inside `APP_B64` / `APP_B64_NEW` in dHUB's `<script>` block. When you open an app, it is decoded with `decodeB64App()` and rendered in an `iframe.srcdoc`. This means:

- **dHUB alone** = complete product (no folder structure needed).
- **Individual app files** = standalone versions, kept manually in sync.
- When you change an individual app file, you must **re-run the Python embed script** to regenerate dHUB.

```
The_Hub/
├── dHUB.html                                 ← self-contained, ~11.6MB
├── hub-shell.html                            ← source-of-truth shell
├── embed.py                                  ← build script
├── Echo/
│   └── echo.html
├── Dora/
│   └── dora.html
├── Labmate/
│   ├── labmate.html
│   └── RDKit_minimal.js / .wasm             ← ORPHANED (no longer referenced by labmate.html)
├── Blueprint/
│   └── blueprint.html
├── Helix/
│   └── helix.html
├── Protein_Tools/
│   └── protein_tools.html
├── BCA/
│   └── bca.html
├── LDI/
│   └── ldi.html
├── Iceberg/
│   └── iceberg.html
├── Cuppa/
│   └── cuppa.html
├── Fabricata/
│   └── fabricata.html
├── Beacon/
│   └── beacon.html
├── Lumina/
│   └── lumina.html
├── Ribbon/
│   └── ribbon.html
├── Arc/
│   └── arc.html                              ← hidden from the home grid, still built
├── Archive/
│   └── archive.html
└── Cell_Archive/
    └── cell_archive.html
```

### Regenerating the self-contained dHUB after app changes

**`embed.py`** reads from `hub-shell.html` and fills in each app's base64. Run from `The_Hub/`:

```bash
python3 embed.py                      # → dHUB.html  (local/offline use)
python3 embed.py dist/index.html     # → dist/index.html  (CI/Pages build)
```

The key regex is `[^"]*` (not `[A-Za-z0-9+/=]+`) to avoid the PLACEHOLDER suffix bug. `embed.py` fails loudly (exit 1) if a source file is missing or a key doesn't match exactly one placeholder.

### Shared curve-fit engine (Echo is canonical)

There is no module system, so the 4PL Levenberg-Marquardt fitter (`_lmFit`, `_solveLin`, `_matInv`, `_fitBest`, `_4plVal4`/`_gain`, `_4plJac4`/`_gain`, `_xAtYMid`, `_tQ95`) is **duplicated** in **Echo** (canonical), **Beacon**, and **Lumina**. As of v1.4.0 (2026-07-13) all three are **in sync** — Beacon/Lumina's copies were reconciled onto Echo's (verified fit-for-fit identical: Beacon in-browser maxParamDiff=0, Lumina Node A/B maxParamDiff=0). Two scripts maintain this:
- **`check_shared.py`** — read-only drift monitor (`python3 check_shared.py`, exit 1 on drift). Run after editing any fit function.
- **`sync_fit_engine.py`** — copies Echo's canonical versions into Beacon/Lumina (`--check` for dry run).

Workflow: **edit the fit math in Echo only**, then `python3 sync_fit_engine.py` to propagate, then verify fits numerically (outputs *can* change if you altered the actual math), then `python3 embed.py`. Neither script is wired into `embed.py`'s build gate.

### GitHub Actions auto-deploy

**Repo:** `https://github.com/maciciorjon-hash/thehub` (private)  
**Pages URL:** `https://maciciorjon-hash.github.io/thehub/`

On every push to `main`:
1. GitHub Actions runs `python3 embed.py dist/index.html`
2. Deploys `dist/` to GitHub Pages
3. dHUB is live at the Pages URL within ~2 min

**Local dev workflow:**
```bash
# 1. Edit any standalone app file
# 2. Rebuild locally
python3 embed.py
# 3. Open dHUB.html to test
# 4. Push → Pages auto-rebuilds
git add Echo/echo.html hub-shell.html CLAUDE.md
git commit -m "Fix: description"
git push
```

**Files tracked in git:** `hub-shell.html`, `embed.py`, `.gitignore`, `.github/`, all standalone app HTMLs, `CLAUDE.md`, `database.rules.json`/`firebase.json`/`.firebaserc` (Firebase RTDB security rules, deployable via `firebase deploy --only database` if the CLI is installed — see Firebase integration section)  
**Files NOT tracked:** `dHUB.html` (generated), `dist/`, `Labmate/RDKit_minimal.*`

### dHUB shell structure

```
hub-shell.html / dHUB.html
├── <script>APP_B64{echo,deg,lm}</script>         — base64-encoded app HTML
├── <script>APP_B64_NEW{dna,pd,pt,spectra,ldi}</script> — base64-encoded app HTML
├── #hub-nav       — nav bar: d logo + "dHUB" + theme toggle + lab/settings btns
├── #hub-announce  — fixed banner below nav (Firebase-driven, admin posts, all sessions see it)
├── #hub-home      — 42px title + rotating subtitle + 8-card grid
├── .app-view × 8  — position:fixed overlays (z-index:10), always in DOM at opacity:0
├── var APP_INFO   — map: id → {letter, color, name} for all 8 apps
├── decodeB64App() — UTF-8 base64 decoder
├── openApp()      — fades in app view
├── backToHub()    — fades out app view
├── HUB_SUBS[]     — rotating subtitles (5s interval, crossfade)
├── Firebase auth  — Google sign-in; isAdmin = user.email === 'maciciorjon@gmail.com'
├── Firebase SSE   — /labconfig.json (lab card visibility) + /announcement.json (banner)
├── applyLabConfig() — hides/shows cards for non-admin; admin always sees all
├── Lab panel      — admin only: per-app toggles + announcement input + preview lab view
└── easter egg     — 5-click on d logo (when on hub home)
```

**Navigation:** dHUB's d logo (`#hub-logo`) acts as back button when inside an app — no button injected into app iframes.

### Animation design

App views are `position:fixed; inset:0; z-index:10`. Opening: overlay fades in (150ms), hub chrome hidden after 200ms. Going back: hub chrome restored immediately, overlay fades out. No blank frames.

---

## Firebase integration

**Project:** `thehub-f80ae` (europe-west1)  
**DB:** `thehub-f80ae-default-rtdb.europe-west1.firebasedatabase.app`  
**Auth domain:** `thehub-f80ae.firebaseapp.com`  
**API key:** `AIzaSyBpw9UbXnCciIi7VBapBeBJOq9U7RSS4g8`

**Admin auth:** Google sign-in via Firebase Auth compat SDK. Admin = `maciciorjon@gmail.com`. Sign in from Settings panel (gear icon). Session persists across reloads.

**Lab config SSE** (`/labconfig.json`): controls which app cards are visible to non-admin users. Admin writes via `firebase.database().ref('/labconfig').set(...)` (Firebase JS SDK, authenticated by the signed-in admin's real ID token). All sessions receive updates in real time via `EventSource`.

**Announcement banner** (`/announcement.json`): admin posts a message from the Lab panel via `firebase.database().ref('/announcement').set(...)`; appears as a fixed 40px banner below the nav for all active sessions. Dismissible per browser session (tracked via `sessionStorage`). Posting an empty string clears the banner.

**Authorized domains** (Firebase console → Auth → Settings): must include `maciciorjon-hash.github.io` for Google sign-in to work on Pages.

**Security (fixed 2026-06-22, see Round 92 session log):** writes used to go through a hardcoded legacy RTDB "database secret" embedded directly in client JS — a platform-level admin-bypass key with no expiry, no rules enforcement, and (since it was server-bundled into a public GitHub Pages page) effectively public. Replaced with real Firebase Auth SDK writes (`firebase.database().ref(path).set(...)`) tied to the signed-in admin's actual identity; rules are tracked in `database.rules.json`/`firebase.json`/`.firebaserc` at repo root (paste into Firebase console → Realtime Database → Rules, or `firebase deploy --only database` if the CLI is installed). **Known unresolved limitation:** the leaked legacy secret itself cannot be revoked or rotated — Firebase removed all console UI for managing legacy RTDB secrets years ago, with no replacement, for every project. It remains permanently valid and permanently bypasses rules on this specific database instance (`thehub-f80ae-default-rtdb`) regardless of what rules say. Jon explicitly accepted this residual risk rather than migrating to a new database instance — don't re-flag this as a fresh finding without checking this note first.

---

## Banner design system

All apps share a consistent header:

| Element | Style |
|---------|-------|
| Logo box | 32×32px, `border-radius:7px`, `font-family:var(--mono)`, `font-size:20px`, **`font-weight:700`**, `color:#fff` |
| App name | `font-size:15px; font-weight:600; letter-spacing:-0.2px` |
| Subtitle | `font-size:11px; color:var(--text2)` |
| Header height | 58px |
| Header layout | `display:flex; align-items:center; gap:14px; padding:0 28px` |

---

## dHUB home design

- Title: **42px, font-weight:700, letter-spacing:-1.2px**
- Rotating subtitle: `font-size:14px`, `color:var(--text2)`, crossfade every 5s
- Cards: 280px min-width grid, 48×48px logo boxes, `font-weight:700` on logo letter
- Max-width: 880px centered

---

## Design system (Echo-style)

All hub chrome uses Echo's exact CSS variables and IBM Plex fonts:

```css
/* Dark */
--bg:#0d0f14  --surface:#13161e  --surface2:#1c2030  --surface3:#252a3a
--border:rgba(255,255,255,0.07)  --border2:rgba(255,255,255,0.13)
--text:#e8eaf2  --text2:#8b90a8  --text3:#4e5368
--accent:#ff5760  --accent2:#0079b9  --accent3:#00c896

/* Light */
--bg:#f4f5f8  --surface:#ffffff  --surface2:#f0f1f5  --surface3:#e4e6ee
--border:rgba(0,0,0,0.07)  --border2:rgba(0,0,0,0.13)
--text:#1a1d2e  --text2:#5a5f7a  --text3:#9ca0b8
```

**Default theme: light** (`<html data-theme="light">`)

---

## How to add a new app

### 1. Build the app HTML file (standalone)

Create `MyApp/myapp.html`. Use IBM Plex Sans/Mono, Echo palette CSS vars, 58px header.

### 2. Add app card to `#hub-home` in `hub-shell.html`

```html
<div class="card" tabindex="0" data-app-id="myapp" onclick="openApp('myapp')" onkeydown="if(event.key==='Enter')openApp('myapp')">
  <div class="card-header-row">
    <div class="card-logo" style="background:#COLOR;">X</div>
    <div class="card-name">My App Name</div>
  </div>
  <div class="card-desc">Short description.</div>
  <div class="card-foot"><span>Tag1 · Tag2</span><span class="card-arrow">&#8594;</span></div>
</div>
```

### 3. Add app-view iframe in `hub-shell.html`

```html
<div class="app-view" id="view-myapp">
  <iframe id="frame-myapp" sandbox="allow-scripts allow-same-origin allow-downloads allow-forms allow-modals allow-popups allow-top-navigation-by-user-activation"></iframe>
</div>
```

### 4. Add entry to `APP_INFO` and `APP_B64_NEW`

```js
var APP_INFO = { ..., myapp: { letter:'X', color:'#COLOR', name:'My App' } };
var APP_B64_NEW = { ..., myapp: "PLACEHOLDER_MYAPP" };
```

### 5. Base64-encode and embed

```bash
python3 embed.py
```

---

## Technical notes

**Self-contained embedding:** Apps stored as UTF-8 base64 in `APP_B64` / `APP_B64_NEW`. Decoded at runtime via `decodeB64App()` using `atob()` + `TextDecoder`.

**LabMate RDKit (removed):** LabMate no longer uses RDKit — `labmate.html` has zero references to it (the chemistry that needed it went with the removed PROTAC Tools section). The `Labmate/RDKit_minimal.js`/`.wasm` files on disk are orphaned (and gitignored, so not in the repo or the built Hub). The old `<base href>` injection note no longer applies; safe to delete the local assets.

**Same-origin srcdoc:** `srcdoc` iframes with `allow-same-origin` are same-origin as dHUB. `localStorage` and `window.parent` calls work.

**LabMate active sections (v0.9.96):** Favourites · Calculators · Mol Biology · Cell Biology · CRISPR · Proteomics · Biophysics · Struct Bio · Genomics. PROTAC Tools and Reference removed.

**Plate Designer mobile:** `.sel-toolbar` anchored to `top:58px` on mobile with `max-height:calc(100vh - 80px); overflow-y:auto` so it never covers the plate canvas.

**Favicon:** SVG data URI in `hub-shell.html` `<head>` — dark rounded square with white "d", matches nav logo.

**`labBtn.style.display`:** Must be set to `'inline-block'` (not `''`) — a CSS rule hides it by default and `''` doesn't override it.

---

## Session log
<!-- AUTO-UPDATED by .claude/stop-hook.sh — do not edit this section manually -->
<!-- LAST_SESSION_START -->
Last session: 2026-07-18 (Labbook v1→v2 — 3-level hierarchy + Word-like ELN overhaul; v1.3.9)
Hub apps: 20. Version v1.3.9.
**Labbook rebuilt from a 2-level notebook into a full daily electronic lab notebook** (`Labbook/labbook.html`, admin-only). Read Jon's real OneNote (via Chrome + his screenshots) and mirrored its organization, then made it highly editable per his spec. Two waves, same work-day, one version bump.
**(1) 3-level hierarchy.** Data model gained `projects[].sections[]` + `experiment.sectionId`; left pane became an OneNote-style **tree** (JOURNAL · GENERAL · PROJECTS; projects expand to experiment-type folders). Orphan experiments migrate to an "Unclassified" folder (no data loss). Seeded Jon's real skeleton on first run (8 projects → their folders; general Notes / Papers I read / Bio Safety Committee / Seminars), guarded by `_seeded` (verified no double-seed on reload). **Seminars** is a master folder with a speaker/title/date template. Excluded per Jon (covered elsewhere): Primers&Plasmids → Plasmids app, Notebook → the daily journal, Protocols → Archive. Code format kept `TYPE+YYYYMMDD` (e.g. `WB20260718`).
**(2) Word-like v2 overhaul.** Persistent **horizontal ribbon** acting on the focused editor (`_activeEd`): B/I/U/S, headings/quote/code, font size, text color + highlight, bullet/number/checklist, indent/outdent, align L/C/R/justify, sub/sup, table, divider, undo/redo, clear. **Resizable** panes (drag splitters, persisted in `localStorage['lb_layout']`; verified via dispatched mouse events 210→330 px — synthetic `left_click_drag` doesn't fire the doc-level mousemove, real mouse works) + **collapsible** to a thin rail. **Movable/dockable panels** (Insert / Outline / Tags / Info) that swap between the ribbon and a right dock (⇄ button + HTML5 drag), persisted. **@ / # / slash autocomplete** in every editor: `@` → experiments/pages/plasmids link (`lb-link[data-exp|data-page|data-app]`), `#` → tags (chip + `data.tags` + pane filter), `/` → insert-block menu. **Daily notebook = day dashboard**: a date auto-aggregates every experiment day-block with that date, edited by *shared reference* so editing in the daily view mutates the experiment's own block and vice-versa (verified bidirectional). **Editable multi-day presets** (`data.presets`, seeded from the old hardcoded templates with a per-block `dayOffset`; on create `block.date = startDate + dayOffset`, so blocks auto-populate the matching daily-notebook dates) + a **preset editor** modal. **Unified PDF export** (`buildPrintDoc` → `#print-root` + `@media print`) for experiments / daily-notebook / pages / seminars: header with code, title, project › folder breadcrumb, meta chips, green day-badged blocks, footer. **Offline** (localStorage-first + online/offline indicator) + **automatic daily backups**: writes `labbook-YYYY-MM-DD.json` via the File System Access API through a new top-level **`window.lbBackup`** bridge in `hub-shell.html` (the sandboxed iframe can't run the picker; right-click the Backup button to choose the folder once — handle persisted in IndexedDB), with a dated-JSON **download fallback** (verified). New `Labbook/backups/` folder (README + `.gitkeep`; `Labbook/backups/*.json` git-ignored).
**Verified in Chrome** (standalone + embedded dHUB, zero console errors): tree + seed with no duplicates, multi-day preset (Day badges + computed dates), day-dashboard bidirectional edit, @/#/slash popups, movable-panel swap ribbon↔dock, collapse + splitter resize, preset editor, PDF preview layout, backup download fallback + `window.lbBackup` present in dHUB. `python3 embed.py` rebuilt clean.
**Labbook v2.1 refinements + Archive integration (same day, still v1.3.9 — folded per the one-bump-per-day rule).** Jon reviewed v2 live and sent a batch of fixes/features; verified ONCE at the end of the batch per his instruction (save tokens). **Ribbon:** Insert group now inline-sized (no bordered card in the ribbon); redesigned colour picker (moved to `position:fixed` — the swatch was being clipped by `#ribbon{overflow-x:auto}`, a real bug; labelled grid + None/clear); **font-family** (Sans/Serif/Mono) + **numeric font-size** (px span-wrap) selectors; sub/superscript made visually distinct (X₂ vs X²); the header actions (online·Backup·dock·Export) **moved into the ribbon** and Labbook's `<header>` is now hidden when embedded (`hub-shell` labbook `embedStyle`→`header{display:none}`) — the two-banner empty space is gone. **Model:** removed the POI/target field (Jon: risky — none/multiple); **+Experiment → +Add** menu (Experiment/Meeting/Note); **no-code note entries** (Meeting/Note = title+date+free body, `kind:'note'`); per-experiment **copy + delete-with-confirm** buttons; **empty title → big code**; **auto-classify a folder by experiment type** when it holds >1 type (WB / HiBiT headers). **Antibody DB:** new admin Antibodies GENERAL section (`kind:'antibodies'`, `{name,catalog,host,dilution,usedIn}`), α-convention (WB preset now α-HaloTag/α-NanoLuc/α-HA), insert via Insert-panel α button + `@` pool; inserting records `usedIn` for **traceability** ("Used in: <codes>"). **Reverse Transfection → Archive** (Jon: goes in Archive, not Labbook; "Archive↔Labbook integration is key"): new `revtx` protocol in `Archive/archive.html` (cellbio, Protocol/Calculate/Output) with `calcRevTx`/`generateRevTxSteps` — plate-format (6/12/24/96) + N-wells → per-well + master-mix(N+1), factors `{96:.1,24:.5,12:1,6:2.5}`, **OptiMEM fixed 100 µL** (verified: 12-well 800 ng/2.4 µL/100 µL; 6-well ×2.5 → 2000 ng/6 µL; 13 rxn → 1.3 mL/26 µg/78 µL). **Labbook↔Archive bridge:** Archive publishes `window.parent.ARCHIVE_INDEX` on load (32 protocols) + already had a `switchTab` deep-link handler; `hub-shell.openApp(id,tab,item)` relaxed to send on `item` alone; hub-shell **preloads Archive** (~2.6 s) so the index is ready; Labbook inserts a protocol chip (`lb-link[data-app=protocols data-pid]`) that deep-opens Archive at that protocol (verified end-to-end). **Notebook:** per-project colours (`project.color`), an "★ Ongoing experiments" view grouped by project, day-dashboard now groups a day's blocks **by experiment** (one code per card), day asc/desc **sort toggle**. **Global:** **⌘/Ctrl+K spotlight** — Labbook-level palette (experiments/pages/tags/folders/antibodies) + a dHUB-level palette in `hub-shell` (reuses `HUB_SEARCH_INDEX`); **right-click context menus** in Labbook (projects/folders/sections/experiments/blocks/days via a shared `popOpen`); wider fluid editor (`min(1060px,100%)`); mobile breakpoints (dock as drawer, panes narrow, bigger touch targets). **Preset editor redesigned** (rich `.rt` contenteditable per block + day stepper + move/remove, no more raw-HTML textareas). Files: `Labbook/labbook.html` (full rewrite), `Archive/archive.html`, `hub-shell.html`; rebuilt `dHUB.html`. **Verified in Chrome** (standalone + embedded, zero console errors): ribbon/fonts/size/colour-picker, +Add/Meeting/no-target/code-big, auto-classify (WB+HiBiT), antibody DB + α, reverse-tx calc scaling in Archive, ARCHIVE_INDEX + deep-link open, Labbook & dHUB Cmd+K, preset editor. No version bump (same day); extended the v1.3.9 changelog + this entry.

**Labbook v2.2 — "OneNote-like" polish + editor debug (same day, still v1.3.9 — folded per the one-bump-per-day rule).** Jon reviewed v2.1 live: felt **over-compartmentalised**, and the Word-like editor was buggy (links/tables/Archive links broken, lists/checklist broken, sub/sup dead, no Tab, no Cmd shortcuts, confusing "online" pill, couldn't delete sub-sections). All in `Labbook/labbook.html`. **Root cause of "can't delete" + "horrible Chrome pop-up":** every delete/rename/insert used native `confirm()`/`prompt()`, which Jon's environment (sandboxed embed / Chrome "prevent additional dialogs") **silently suppresses** → `if(!confirm())return` always bailed and `prompt()` returned null (nothing inserted). **Fix (foundation):** built DOM-based **`lbConfirm`/`lbPrompt`/`lbPicker`** (reuse `.modal-back/.modal` CSS, new `#lb-dialog`) and replaced ALL native dialogs (delProject/Section/General, rename*, delExperiment, delDay, abDel, openAntibodyIns→searchable antibody picker, openProtocolIns→searchable Archive picker). Inserts now save/restore the caret range (`_saveSel`/`_restoreSel`, `insertHtmlAtCaret(html,range)`) since a modal steals focus. Verified deletion works **embedded in dHUB's sandboxed iframe** (the exact scenario that was broken). **Editor keyboard** (new delegated `keydown` on `.rt`): **Tab/Shift+Tab** = indent/outdent (styleWithCSS margin, real tabulator), **⌘/Ctrl+B/I/U** explicit, undo/redo left native. **Checklist rewritten** from a `.lb-todo` `contenteditable=false` box island (the "bullets rotos, cursor descolocado" cause) to a **real `ul.lb-check`** with the checkbox drawn on `li::before` + a check via `::after`; click-toggle detects the marker zone (`clientX-rect.left<=26`), works at any nesting depth (`closest('ul.lb-check')`); legacy `.lb-todo` still supported for old data; print CSS updated. **Tables:** replaced the fixed 2×2 insert with a **Word-style size-grid picker** (`#tbl-grid`, hover N×M) inserting a table with a `<th>` header row, + a **floating contextual table toolbar** (`#tbl-tools`, shown on `selectionchange` when caret is in a `.rt table`) — insert/delete row/col, toggle header, delete table (buttons use `onmousedown+preventDefault` to keep the caret). **Sub/superscript fixed** via `fmtSubSup` (forces `styleWithCSS false` → real `<sub>/<sup>`) + explicit `.rt sub/sup` CSS. Added a **Web link (URL)** Insert entry (`openLinkIns`, lbPrompt) alongside the internal `@`-mention. **Experiment editor** (Jon chose "canvas + dated blocks"): a **free-form OneNote-style canvas** (`e.html`, `setField('html')`→`queueSave`) above the dated blocks, which now live in a discrete collapsible `<details class="exp-dated">`; canvas included in PDF export. **Antibodies** reworked to a **data-entry form** (name/catalog/host/dilution/target + "＋ Add", Enter-to-add, `abAddFromForm`) above a clean list; the redundant middle "pages" pane is **hidden** for the antibodies section (`pane-pages-wrap` display:none). **Daily notebook** "From experiments this day" → **"What to do today"**: each card shows the **project name** (in project colour) + the experiment **code as a deep-link** (`openExp`) + block titles as deep-links (`openBlk`=openExp+scrollBlk), mirroring Jon's real OneNote; `@`-mention exp links now prepend the project. **"online" pill → clear save status** (`status-pill` merging net+sync: "Saved ✓" / "Saving…" / "Offline", `updateStatus`). **Verified in Chrome** (standalone localhost + embedded dHUB, zero console errors): every editor op (bold/lists/checklist incl. nested/table+header/sub-sup), Tab-indent via real keys, table toolbar add/del row-col + header toggle, section delete via custom dialog **embedded**, antibody form add + hidden middle pane, daily project-labels + deep links, free canvas + collapsible dated sections, insert pickers/spotlight/status pill. Files: `Labbook/labbook.html`, `hub-shell.html` (changelog bullet extended), rebuilt `dHUB.html`. No version bump (same day).

Previous session: 2026-07-17 (Labbook v1 + Plasmids app + Gibson calc fix; grouping made admin-only + visitors back to per-app unlock; still v1.3.8)
**Archive — all calculators, responsive input layout (2026-07-17):** Jon: "espacio óptimo en todas las calculadoras." Root cause of wasted space = calculators used a fixed `.grid-2` (2 columns) for their input `.field`s, so on the wide calc-box the fields were over-wide and stacked into many rows (e.g. HiBiT Lytic's 7 fields → 2×4). Fixed with ONE scoped CSS rule — `.calc-box .grid-2{grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px 14px}` — so fields pack to fill the width across all 30 calc-boxes. Safe because there's no `.grid-3` and no `grid-2` in JS result markup (grep-confirmed), so only calculator inputs are affected; nested grid-2 (ligation's Vector|Insert with inner size/conc) still groups correctly. The Gibson calc was additionally flattened by hand (Vector|Settings two-column split → one responsive input row + a separate "Insert fragment(s)" section with compact 160px fields). Verified HiBiT Lytic + Ligation live.

**Archive — Gibson calculator reworked (2026-07-17):** Jon flagged the vector/insert amounts + ratio were wrong and that he standardly uses **35 ng vector**. `calcGibson()`/`generateGibsonSteps()` (`Archive/archive.html`) now drive off a **Vector amount (ng)** input (default 35, was a fixed 0.06 pmol target) → vector pmol = ng/(0.65×bp); a **user-selectable Insert:vector molar ratio** (`#gb-ratio`: 1:1/2:1/3:1/5:1, default 2:1) sets insert pmol = vector pmol × ratio; result table + CSV/Output/PDF now show an **Amount (ng)** column and the ratio/vector-target line. Guard notes for DNA-volume-exceeds-half-reaction and total-pmol-above-NEB-window. **DNA amounts scale with the reaction volume**: lab standard is 35 ng vector in a **10 µL** reaction, and `gibScaleVector()` re-derives vector ng = `35 × vol/10` when the `#gb-vol` field changes (5 µL → 17.5 ng, 20 µL → 70 ng), then recomputes (insert + MM scale too); the ng field stays manually editable until the volume changes again (`GIB_STD_VEC_NG=35`, `GIB_STD_VOL=10`). Verified live (10→35, 5→17.5, 20→70 ng; 2:1 → 8.75 ng insert at 10 µL). `NG = pmol × bp × 0.65` (650 g/mol/bp) — the canonical dsDNA factor for this file now.
Hub apps: 18 (13 public unlocked individually + Bench/Labbook/Plasmids/Cell Archive admin-only + Fabricata easter egg). Version v1.3.8.
**Plasmids app** (`Plasmids/plasmids.html`, id `plasmids`, admin-only, indigo `#6d7bd0`, in Bench suite) — separate plasmid database per Jon's plan. Searchable list + detail editor: `pJM##` auto-incremented code (`nextCode()` scans existing), name, backbone, insert, resistance, tags, POI, source, status (idea/cloning/sequencing/verified/failed, colored dot), sequencing notes, notes, + **SnapGene map image**. **Map images are client-side compressed** (`compressImage`: canvas max 1600px, PNG, JPEG 0.9 fallback if >900KB) and stored as **data URIs in `/journal/plasmids`** (Firebase via `window.parent.firebase`, debounced) + localStorage (**quota-safe**: `saveLocal()` drops `mapImage` from the local cache if over quota, keeps metadata; images reload from Firebase). **Works now with NO Firebase Storage activation** (base64). Map lightbox, CSV export, live search. `storage.rules` added at repo root (admin-only `/plasmids`,`/labbook` paths) for WHEN Jon enables Storage — a console-only step I can't do for him; base64 works meanwhile. Embed header special-case now covers `labbook`||`plasmids` (keeps Export button). Verified in Chrome (auto codes, fields, map thumb+upload, search, persistence, parent-FB) — zero console errors.
**Two things this session.** (1) **Grouping is admin-only now** (per Jon): `renderSuites()` dispatches — admin → suite cards + drill-in (Data Analysis / Archive / Design / Tools / Bench); **visitors → individual app cards + per-app discover code** (restored `_unlockedApps`/`APP_UNLOCK_WORDS`/`tryUnlock`). Cell Archive is admin-only (`ADMIN_ONLY_APPS={cellarchive,bench,labbook}`); cryo/cuppa stay public individually but sit under Bench in Jon's view; Fabricata = hidden easter egg (`FABRICATA_WORD='fabricate'` opens it directly). **Single-app suites open their app directly** — `openSuite()` short-circuits to `openApp()` when a suite has one visible app (so Archive, which only holds `protocols`, opens the Archive app on click instead of a pointless one-card drill-in); multi-app suites still drill in. (2) **Labbook v1** (`Labbook/labbook.html`, id `labbook`, admin-only, in the Bench suite, teal) — the "eje central" of Jon's personal hub, an improved OneNote copy.
**Labbook build** (modelled on Jon's real OneNote — read 6 exported PDFs first via `pypdf`, installed `--user`): **3-pane** layout (Projects + Notebook | pages list | rich-text editor). **New experiment** → pick type (WB/HB/CTG/NB/KD/D2B/PR/CLONE/custom) + POI prefix + date → **auto code** matching his convention (`TYPE+YYYYMMDD[_POI]`, e.g. `HB20260716_SMARCA`; D2B special-cased to `POI_D2B_DATE` like `SM_D2B_20260515`) + a **unified header** (code/title/type/status/start-date/POI/plasmids/cell-lines) + a **type-specific template of dated day-blocks** pre-filled from his real protocols (HiBiT lytic recipe, CTG readout, NanoBRET transfection, cloning linearization/HiFi/transformation/miniprep, etc.). **Word-like editor** = `document.execCommand` toolbar (bold/italic/underline/strike/H3/bullet/number/checklist/highlight/sub/superscript/clear) + **insert dHUB-app links** (`a.lb-link[data-app]` → `window.parent.openApp`). Checklist items = `.lb-todo` with a clickable `.lb-box` (contenteditable=false) toggled via delegated click. **Daily Notebook** = dated pages (with a Cell-check block seed). **Storage**: localStorage-first (`hub_labbook`, offline/reliable) + **debounced Firebase sync** to `/journal/labbook` via `window.parent.firebase` (adopts remote on load if newer by `.updated`). **PDF export** = print-based `#print-root` (code + title + meta + dated blocks) → `window.print()`. Wired: `embed.py`, `APP_B64_NEW` placeholder, `APP_INFO.labbook`, hidden admin card, Bench suite (first). **Embed header special-case** (`_loadApp` embedStyle) keeps Labbook's header bar (hides only logo/name) so the Export button stays reachable — like beacon. **Verified in Chrome**: type templates + code formats (HB/D2B/CLONE), editor render, notebook day, PDF HTML build, localStorage persistence, parent-Firebase binding, in Bench drill-in; zero console errors. Note: the app-view fade-in `requestAnimationFrame` is throttled to 0 opacity when the tab is backgrounded in automation (not a real bug — `display` is synchronous; works on a focused tab). **NEXT (Jon's roadmap, not started):** a separate **Plasmids app** (pJM## DB + SnapGene maps — needs Firebase **Storage** activated, explained to Jon as free-tier; he's OK to activate when we get there); flesh out Labbook (notebook↔experiment links, embed Archive-calculator results, richer templates from more PDFs, per-block reordering). The `/journal` Firebase rules (now also covering `labbook`/`layout`/`shortcuts`) still need pasting in the console.

Last session: 2026-07-16/17 (Workspace reshape — suite cards + admin widget dashboard + live weather; still v1.3.8, same continuous session)
Hub apps: 16 (15 public across 5 suites + Bench, admin-only). Version v1.3.8.
**Reshaped dHUB from a flat app grid into a "suite launcher + personal workspace" — one product, split by who's viewing.** Jon's direction: keep dHUB a public multi-app site, but for him make it a full work suite. Visitors now **unlock a whole SUITE by one code** (not per-app), and the home is **suite cards that drill into their apps**. The admin (Jon) additionally gets an **editable widget dashboard** on top.
**Suite model** (`SUITES` in `hub-shell.html`, replaces `PACKAGES`): Data Analysis (`analyze`), Design (`blueprint`), Molecular Biology (`benchwork`), Lab Operations (`inventory`), Just for Fun (`fabricate`), + admin-only **Bench/personal**. `renderSuites()` builds `#hub-suites` (locked vs. unlocked card states); `tryUnlockSuite()` persists to `localStorage['hub_unlocked_suites']`; `openSuite()`/`closeSuite()` swap `#hub-suites` for `#hub-suite-detail`, which **clones the app cards from the now-hidden `#app-grid` source template** (kept in the DOM only so `enterAppNav` can still clone each app's SVG crumb icon). Per-app unlock (`APP_UNLOCK_WORDS`/`_unlockedApps`) and the whole `_buildPackages`/package-grid/card-order machinery are now dead code (left in place, unused); `applyLabConfig()` → `renderSuites()`; search gating → new `_isAppAccessible()`; the Settings "Card order" row was removed.
**Widget dashboard** (`#hub-widgets`, admin-only): a `wg-grid` (2-col) of widgets — `greeting` (the sky hero, fixed full-width, non-removable), `tasks`, `incubator`, `shortcuts`. **Edit mode** (`toggleWidgetEdit`): drag-reorder via HTML5 DnD with a **FLIP animation** (`_flipMove` captures rects, mutates DOM, inverts+plays via `.wg-nomove`/transform), a width toggle (1↔2 cols), remove (×), and an add-tile palette; layout persists to Firebase **`/journal/layout`** (JournalStore extended with `setLayout`/`setShortcuts`, and `layout`/`shortcuts` added to its snapshot + cache). Shortcuts persist to `/journal/shortcuts` and open apps directly (picker overlay lists apps by suite). The subscribe callback re-renders widgets/suites only when not mid-drag (`_wgDragEl` guard).
**Time-of-day sky + live weather** (the greeting widget): `_skyScene()` picks a day-part gradient (night/dawn/morning/day/dusk) + a sun/moon positioned along an arc by hour, with CSS-animated rotating sun rays, twinkling stars at night, and drifting clouds when cloudy. **Weather is live** from **Open-Meteo** (`initWeather()`, `https://api.open-meteo.com/v1/forecast?...` — no API key, CORS-enabled, defaults to Dundee 56.462,-2.9707), mapping the WMO `weather_code` + `is_day` to an animated SVG icon (`_wxIconSvg`: sun/moon/cloud/fog/rain-drops/snow-flakes/thunder-bolt) + temp + label; refreshes every 15 min. Sun-vs-moon in the sky follows the API's real `is_day`.
**Verified end-to-end** (local server + Chrome; admin simulated via `isAdmin=true;_applyDashAdmin()` since Google auth isn't available in automation — the exact auth-callback path): visitor sees 5 locked suite cards, no widgets, hidden app grid; unlocking Data Analysis by code drills into its 6 apps; opening an app overlays and `backToHub` returns to the drill-in; wrong code shakes. Admin: 6 suites all unlocked incl. Bench, widget dashboard with default `[greeting:2,tasks:1,incubator:1,shortcuts:2]`; live weather fetched (13.7°C/Cloudy/night → moon+clouds shown); edit mode add/remove/width all mutate + persist to `/journal/layout`; a simulated drag reorder moved a widget and persisted the new order; shortcut add persisted to `/journal/shortcuts`. Zero console errors. Rules note unchanged: the private `/journal` Firebase rules still need pasting in the console (adds `layout`/`shortcuts` under the same admin-only branch).

Last session: 2026-07-16 (Personal command center + Bench app — Jon's private notebook; still v1.3.8, folded into today's entry per the one-bump-per-day rule)
Hub apps: 16 (15 public + Bench, admin-only). Version v1.3.8.
**Personal command center + Bench — turning dHUB into Jon's electronic lab notebook, visible only to him.** Jon wanted dHUB to stay a password-gated public app launcher for visitors, but for *him* to become more: a private daily briefing ("Good morning, Jon") with urgent/pending tasks, plus a cell-biology incubator assistant tracking what's growing (line, passage, last split) and raising "check cell line" alerts. Built entirely off the existing `isAdmin` flag (Firebase Google auth = `maciciorjon@gmail.com`), so nothing about the public unlock flow changed.
**Two surfaces, both admin-only.** (1) A home-page **command center band** (`#hub-dash`, injected at the top of `.home-wrap` above `#app-grid`) — a gradient greeting hero (time-of-day via `getHours()`, fixed Nature blue→teal `#5e87c5`→`#51c3ce` so it reads in both themes), three stat chips (Tasks open · To check · Next split), and two mini panels (top open tasks with inline add/complete + top incubator alerts). Revealed only when `isAdmin` via `_applyDashAdmin()` (called from `onAdminStateChanged` and once in `initHub` for a correct non-admin baseline). (2) A dedicated **Bench** app (`Bench/bench.html`, id `bench`, teal `#4f9d8f`) with **Tasks** and **Incubator** tabs — full task CRUD (priority, due date, filters, inline edit) and a culture tracker (per line: passage, last split, interval, passage limit, media, vessel, myco date, notes; actions Log split [passage+1, resets clock, appends history] / Log myco test / Edit / Remove with inline confirm; add-form pre-fills from the reference table).
**Data model — one shared store, private Firebase path.** `window.JournalStore` (in the shell) owns a single Firebase RTDB listener on **`/journal`** (private) via the authenticated SDK `firebase.database().ref('journal').on('value',…)` — NOT the public REST `EventSource` used for `/labconfig`, because this is Jon's private data. Mirrors to `localStorage['hub_journal_cache']` for instant paint / offline; writes are optimistic-local + `_write().catch()` (so a denied write before rules are deployed just keeps the cache). Shape: `/journal/tasks/<pushId>{text,priority,done,created,due}` and `/journal/incubator/<pushId>{line,passage,seeded,intervalDays,passageLimit,media,vessel,mycoLast,notes,ratio,type,history[]}`. Bench is a same-origin `srcdoc` iframe, so it uses `window.parent.JournalStore` / `window.parent.CELL_REF` / `window.parent.computeCellAlerts` (single source of truth, one listener) and falls back to local copies only when opened as a standalone file. **`database.rules.json` gained a `journal` branch** with `.read`+`.write` both restricted to the admin email (unlike world-readable labconfig/cuppa) — **must be pasted into the Firebase console** (Realtime DB → Rules) to take effect; until then the band shows the localStorage cache only.
**Alert logic + reference table.** `computeCellAlerts(cell,now)` returns prioritized alerts: split due/overdue (from `intervalDays`), high/approaching passage (from `passageLimit`, warn at ≥limit, soft at ≥80%), mycoplasma overdue (>30d or never). `CELL_REF` bakes in 11 common lines (HEK293/293T, HeLa, U2OS, A549, HCT116, MCF7, Jurkat, NIH/3T3, CHO, Expi293) with typical ratio/interval/doubling/passage-limit/media — general guidance, overridable — used to pre-fill new cultures and drive thresholds. "Info from the internet" is a noted future enhancement (the site is static; can't live-fetch yet).
**Wiring.** `bench` added to `embed.py` APPS + `APP_B64_NEW` placeholder + `APP_INFO` + a new **`Personal`** package (first, so Bench sits under the command center for admin). Card carries `data-app-id="bench" style="display:none"` and is NOT in `ALL_APP_IDS`/`APP_UNLOCK_WORDS` (no unlock word — gated by auth, not discovery); `_applyDashAdmin` toggles its display and `_updateSectionVisibility` auto-hides the empty Personal section for non-admins. Kept out of `HUB_SEARCH_INDEX` so visitors never surface it. Changelog: one bullet folded into the existing v1.3.8 entry (no new version — same continuous session).
**Verified end-to-end** (local server + Chrome, admin simulated by forcing `isAdmin=true; _applyDashAdmin()` since real Google auth isn't available in automation — this is exactly the code path the auth callback runs): non-admin sees no band/Bench card/Personal section and an unchanged grid; admin reveals all three; adding a task from the band and cells with alert scenarios (HEK293T P38 seeded 5d ago + myco 40d ago → three warn alerts; HeLa recent → none) computed correctly; opening Bench bound its store to `window.parent.JournalStore` and its `CELL_REF` to the parent's; Log split took HEK293T P38→P39, reset the clock, appended history; Log myco cleared the myco alert; Jurkat add-form pre-filled interval/limit/media from the reference; all Bench changes propagated live to the home band; data persisted across a full reload via the cache; zero console errors. **Not deployable from here:** the `/journal` Firebase rules paste is a manual console step for Jon.

Last session: 2026-07-14 (Echo backfill vehicle wells + changelog cleanup + rebrand to dHUB + app renames; v1.3.7)
Hub apps: 15. Version v1.3.7. (All of this day's work is one version bump / one changelog entry — see the versioning rule.)
**Echo Gradient Planner backfill calculator now accounts for DMSO-only (vehicle) wells.** Jon pointed out the existing intermediate-plate backfill calculator (`_egBfCompute`, `Labcyte_Echo/labcyte_echo.html`) only summed the DMSO shortfall of compound-dosed wells (`maxT - vAdj` per dose point) — it missed that real runs also include wells filled with **only DMSO** (no compound) at the same max-transfer volume as the highest-dose well, used to homogenize final DMSO% across the whole destination plate (vehicle/homogenization controls). Since those wells get zero compound transfer, their *entire* volume comes from backfill DMSO, not just a shortfall — a bigger contribution than any compound well's partial top-up. Added a new **"DMSO-only (vehicle) wells"** input (per set, default 0 for exact backward compatibility) next to the existing compound/sets/replicates fields; `vehicleBackfillNL = nVehicle*nSets*maxT` is now added to `totalBackfillNL` alongside the pre-existing `compoundBackfillNL`, and vehicle wells are added to `destWellsNeedingBackfill`. Both the live calculator output and the Copy-summary text now break out the compound-vs-vehicle contribution when vehicle wells > 0. Verified via Playwright against the standalone file: with the default gradient loaded, 4 vehicle wells/set at maxT=40 nL added exactly 160 nL (4×40) on top of the existing 775 nL compound backfill for a 935 nL total; leaving the field at its default 0 reproduces the prior numbers exactly (775 nL, 20 dest wells needing backfill) — confirmed no regression for existing users who don't use vehicle wells.
**Changelog cleanup.** The in-app "What's new" panel had drifted back into detailed paragraph-style bullets (v1.1.1–v1.3.7, 25 entries). Jon: "shorten the log entries A LOT... if we have added new stuff write (added new features), if there are bug fixes just write bug fixes." Rewrote that whole range to the terse `<b>App</b>: new features` / `<b>App</b>: bug fixes` / `<b>App</b>: visual updates` style already used for v1.0.95 and older (no mechanism/rationale in-app — that still lives here and in git history). Also caught and merged several same-calendar-day version bumps that had never been day-grouped (29 Jun had 4 separate entries, 24 Jun had 4, 22 Jun had 4, 21 Jun had 3, 20 Jun had 3) into one entry each. Explicitly asked and confirmed: this terseness rule applies to the in-app changelog only, **not** to this Session log, which keeps its detailed style on purpose.
**Rebrand: The Hub → dHUB.** Jon is thinking bigger-picture — positioning this suite as a centralized TPD (targeted protein degradation) ops platform, potentially sellable to other labs, with the Data Analysis pack (Echo/Beacon/Lumina/Degradation Explorer/LDI) as the flagship. First step: rename "The Hub" to "dHUB" (pronounced the same, "d" now standing for Degradation) everywhere user-facing. Changed: page `<title>`, favicon (H→d), nav logo box + `#nav-title`, home hero `.hub-title`, `.opts-version` label, changelog `<b>Hub</b>` mentions, "Back to Hub" tooltips, the Ryan-easter-egg comic's `regg-title`, plus code comments in Cuppa/Iceberg that explained embed behavior in terms of "The Hub." Renamed the build output `The Hub.html` → `dHUB.html` (updated `embed.py`'s default `OUT`, `.gitignore`, `CLAUDE.md`) and the GitHub Actions workflow display names — **none of this touches the actual build target for Pages** (`dist/index.html`), so the live deploy path is unaffected. Deliberately left alone: (1) the GitHub repo name (`maciciorjon-hash/thehub`) and Pages URL (`.../thehub/`) — renaming that breaks the live/shared URL and is an external, harder-to-reverse call Jon hasn't made yet; (2) the local folder name `Desktop/The_Hub` — same reasoning, lower stakes but still worth a deliberate decision rather than a silent mid-session rename; (3) the Firebase project id `thehub-f80ae` — renaming a Firebase project isn't a text edit, it's standing up a new project and migrating data, wildly out of scope for a cosmetic rebrand; (4) historical/narrative content that names the old brand as a period-accurate detail — the Ryan-egg comic's "THE HUB — v1.0.35" screenshot text (explicitly version-pinned to an old release) and LabMate's Pip's Story "Chapter VI · The Hub Era" (narrates the app's actual history chronologically) both keep the old name on purpose, same logic as not retconning a real company's old product name out of its own history page. All of `<b>App</b>: category` changelog bullets that mention "Hub" as one of the affected apps were updated to "dHUB" for the current-and-future entries; the legacy pre-v1.0.95 bundle blocks were left alone (pre-existing, deliberately untouched convention, see the changelog-format memory note). Verified via Playwright screenshot against the rebuilt `dHUB.html`: title/nav/home-title/logo-letter/version-string all read "dHUB" correctly.
**App renames (same rebrand push):** three apps got new short names matching the Echo/Beacon/Lumina/Ribbon/Arc one-word convention — `Labcyte Echo`/`Echo Data Analysis` → **Echo**, `Degradation Explorer` → **Dora** (Jon's own pun, "Degradation" + "the Explorer"), `Lab Designer` → **Blueprint** (covers both its plate-layout and gel-diagram design work). Updated everywhere current: home cards, `APP_INFO`, `HUB_SEARCH_INDEX` (old names kept as extra search keywords so old muscle memory still finds them), the current-range changelog bullets, and each app's own `<title>`/header self-reference (`Labcyte_Echo/labcyte_echo.html`'s `<h1>`/setup-modal title/Guide heading, `Degradation_Explorer/degradation_visualizer.html`'s `<title>` — which had actually drifted to "Degradation Visualizer", a third, inconsistent name, before this fix — and its `updateMeta()` fallback label, `Plate_Designer/plate_designer.html`'s `<title>`/`.app-name`). Left alone on purpose: legacy pre-v1.0.95 changelog bundles and narrative/historical content (same reasoning as the dHUB rebrand), and the on-disk folder/directory names (`Labcyte_Echo/`, `Degradation_Explorer/`, `Plate_Designer/`) — renaming those is a bigger git-mv operation with no user-facing payoff, deferred unless Jon asks.

**MERGED TO MAIN (2026-07-14, still v1.3.7 — no version bump, see the versioning rule):** built out to a full 31-protocol migration (up from an initial 3-protocol proof-of-concept prototype — see below for what changed between passes). `Protocols/protocols.html` (app id `protocols`, accent `#a56983`) is a card-grid-per-protocol app with **Protocol / Calculate / Output** tabs, wired into the shell like any other app (`embed.py`, `APP_INFO`, `ALL_APP_IDS`, `PACKAGES` under Molecular Biology, `HUB_SEARCH_INDEX`, home card, `app-view` iframe, unlock word `benchwork`). Built on `feature/protocols-suite`, fast-forward merged to `main` and pushed once Jon reviewed and asked to publish; the in-app changelog's existing v1.3.7 entry got one added bullet (`New app: Protocols`) rather than a fresh version bump, since v1.3.7 already represents today's work per the one-bump-per-day rule.

**Pass 1 (prototype, superseded):** 3 protocols (Simple Dilution, Gibson Assembly, Western Blot) to prove the pattern before investing further. Jon's review: liked the style, but flagged two concrete problems — "los output son malos, el pdf es muy feo y esta mal" (the outputs are bad, the PDF is ugly and wrong) — and asked for the full migration, excluding LabMate's Quick Calc section (generic calculators like Simple Dilution — those stay in LabMate for now), classified by knowledge branch, combining near-duplicate protocols.

**Root cause of the PDF bug:** jsPDF's built-in fonts only cover WinAnsi/CP1252 — arrows (→), the real minus sign (−), subscript digits (₂), and Greek letters (α) all silently mis-render as garbage glyphs, and — worse — the resulting glyph-width mismatch was corrupting jsPDF's own line-spacing calculations, producing the "letters spread apart" look Jon saw. **Fix: replaced jsPDF entirely with a print-based export** (`exportProtocolPDF` now builds a hidden `#print-root` clone of the Protocol tab + any calculated values, adds a `body.printing` class that a `@media print` block uses to hide everything else, and calls `window.print()`). This gets full Unicode for free from the browser's own text engine — no font table to maintain — and it visually matches the on-screen design exactly since it IS the on-screen markup, just restyled for paper. The "↓ PDF" button is now labelled "🖶 Print / Save as PDF" to be honest about the mechanism. jsPDF's `<script>` tag was removed entirely (one fewer CDN dependency). Verified the exact bug is gone: printed a protocol containing arrows/subscripts/Greek letters (TR-FRET, which has both), confirmed the injected `#print-root` HTML contains real `α` and `<sub>` markup rather than any character substitution.

**Pass 2 (full migration):** re-ran the same content audit (this time reading every remaining section directly rather than via subagent, since I already had the file's structure) and migrated all of Mol Biology, CRISPR, Cell Biology, Proteomics, Genomics, Structural Biology, and Biophysics — **32 protocols total**, organized into 7 category filter pills (dropped the "Calculators" pill entirely, per the exclusion). Removed Simple Dilution from Protocols (it belongs to Quick Calc, which stays in LabMate for now — Jon said "ya veremos cómo organizarlos luego"). **Combined near-duplicates as asked:** LabMate had two separate protocols — "CRISPR KO via PX458 Transfection" and "Knock-in — PX458 + Plasmid Donor" — that shared byte-for-byte identical calculators (`calcBbsIDigest`/`calcPX458HiFi`, both already accepted an id-prefix parameter in the original, evidence they were always meant to be reusable). Merged into one **"CRISPR Cloning (PX458 Vector)"** protocol: one shared Part 1 (clone gRNA into PX458, one calculator), then a "Path A — Knockout" / "Path B — Knock-in" fork covering only the genuinely different downstream steps (transfection reagent amounts, donor design, screening). Confirmed via the code (not guessed) that this was safe: both source protocols called the exact same functions with the exact same math, just different narrative around them. Left "Electroporation (CRISPR RNP)" and "Knock-in — RNP Electroporation + ssODN" as two separate protocols — they share a topic (RNP delivery) but materially different content/specificity (one is the lab's general SOP, the other is HDR-donor-specific design + genotyping guidance), not "practically identical" the way the PX458 pair was. Skipped LabMate's orphaned/dead `proto-ki` (a third, UI-unreachable "combined" Knock-in write-up, confirmed via the earlier content-audit subagent) rather than resurrecting unclear-provenance content.

Of the (then-)32: **9 are calculators** (Gibson, Restriction Cloning, CRISPR Cloning/PX458, BCA & Sample Prep, HiBiT Lytic, CellTiter-Glo 2.0, Transfection, S-Trap, MiSeq PCR) each with a full Protocol/Calculate/Output flow; **23 are reference-only** (no Calculate tab) spanning Transformation/Miniprep/NucleoSpin, gRNA Design/Electroporation/ssODN-KI/Single-Cell Sorting, Immunoprecipitation/HiBiT-Blotting/Lentiviral-Transduction, DIA-NN/Perseus, Gene-Sequence-Retrieval, all 6 Structural Biology protocols (PDB/AlphaFold/PyMOL×2/Pockets/Ternary-Complex-Modelling), and all 3 Biophysics assays (TR-FRET/FP/SPR) — the last two categories are dense, TPD-specific reference material (Cheng-Prusoff, cooperativity α, ternary complex kinetics) that reads as genuinely flagship-quality content for the "sell this to other labs" framing. Output tab now also has a CSV/Excel button (Blob-download, no SheetJS dependency) wherever a calculator produces genuine tabular output (Gibson, BCA, HiBiT Lytic, CTG2, Transfection, S-Trap, MiSeq) — skipped for the two multi-calculator-box protocols (Restriction Cloning, PX458) where "which table" would be ambiguous; PDF/Print and Copy-as-text cover those instead. All 32 verified via Playwright: every protocol opened + (if present) Calculate tab exercised + Output tab exercised with zero console errors; spot-checked math correctness (BCA, Restriction Digest/Ligation, combined PX458 BbsI+HiFi) against the original LabMate formulas; caught and fixed one genuine bug during testing — a copy-pasted emoji in the AlphaFold pLDDT color legend (🟦 light-blue square) had the wrong decimal HTML entity (`&#129462;`, decodes to an unrelated codepoint) instead of the correct `&#128998;` — screenshotted the fix to confirm all four confidence-color emoji now render correctly.

**Pass 3 (visual audit + CRISPR dropdown unification, same review round):** Did a real interactive-browser pass (claude-in-chrome against a local `python3 -m http.server`, not just synthetic Playwright screenshots) over the home grid, dark mode, and several protocols, and fixed what it found: a CSS specificity bug where `.search-box input{padding-left:34px}` was being overridden by a later equal-specificity `input[type="text"]{padding:7px 10px}` (search icon overlapped the placeholder text) — fixed by raising specificity to `.search-box input[type="text"]`; a broken printer emoji on all 32 Output-tab buttons (`&#128438;`, the obscure "PRINTER WITH RIGHT ARROW" codepoint with poor font support → tofu box) → standard `&#128424;` PRINTER emoji; `text-transform:uppercase` was folding the micro sign "µ" to Greek capital Mu, so "20 µL reaction" rendered as "20 ML REACTION" in 3 spots (2× `.calc-box-title` in the PX458 protocol, 1× `.proto-section` in TR-FRET) — replaced `&micro;L` with plain `uL` there, matching the file's dominant convention; and missing CSS for `.proto-part`/`.proto-day`/`.proto-day-badge`/`.proto-day-label` (markup ported from LabMate's PX458 write-up but the styles never came with it), which rendered as unstyled run-together text — added the missing rules. Mid-audit, Jon asked for a bigger structural change: **"unifica los protocolos necesarios y pon dropdown menus... si quieres hacer un knock out, la card es CRISPR knock out y dentro ya elegimos si queremos electroporation o con el plasmido"** — one card per *experiment*, not per delivery method. Replaced the three separate CRISPR cards (CRISPR Cloning/PX458, Electroporation/RNP, Knock-in RNP+ssODN) with two: **CRISPR Knockout** and **CRISPR Knock-in**, each with a `.delivery-bar` dropdown (PX458 plasmid vs RNP electroporation) that toggles paired `.method-branch[data-pid][data-method]` divs across all three tabs (Protocol/Calculate/Output) via a new `setMethodBranch(pid, method)` + `_methodState` map. The PX458 calculators (`calcBbsIDigest`/`calcPX458HiFi`/`renderPX458Export`/`generatePX458Steps`) already took an id-prefix parameter from the Pass-2 merge, so this reused that mechanism with two new prefixes (`ko-`/`ki-`) instead of inventing a new one — each card's inputs and results are fully state-isolated (verified: filling in the Knockout card's calculator leaves the Knock-in card's fields untouched). `genSteps()` for these two cards returns `null` (not `[]`) when the RNP branch is selected, since RNP has no calculator — `refreshExportPanel` was adjusted to treat a falsy return as "no calculator" so the Output tab shows the correct fallback message instead of a misleading "fill in the Calculate tab" note. The delivery-method dropdown itself is hidden from the printed/PDF output (`#print-root .delivery-bar{display:none}`) and the export's `.print-meta` line now states which delivery method was exported — printing already correctly excluded the hidden branch's content for free, since `setMethodBranch` toggles the branches' *inline* `style.display`, which `innerHTML` naturally carries into the print clone. Net protocol count: 32 → **31** (three cards → two). Verified end-to-end in a real Chrome tab: dropdown swaps content on both cards, both calculators compute correctly and independently per prefix, Output tab reflects the active branch (calculator steps for PX458, no-calc note for RNP), zero console errors on load or interaction.

**Pass 3 continued — full interactive visual audit, all 31 protocols:** finished walking every protocol card in a real Chrome tab (not just Playwright) — all 21 reference-only cards' Protocol tab, and all 10 calculators' Protocol/Calculate/Output tabs, including re-verifying the pLDDT emoji fix and the new CRISPR dropdown cards. Zero console errors across the entire pass. **Caught one real math bug along the way**: CTG2's `calcCTG()`/`generateCTGSteps()` (`Protocols/protocols.html`) used `fmt(x, 0)` in several places expecting "no decimal places," but `fmt()`'s second argument is *significant figures* (`n.toPrecision(d)`, `d=Math.max(1,Math.round(d))`) — `fmt(x,0)` therefore rounds to **1 significant figure**, not zero decimals. Harmless when the value already had 1 leading sig fig (e.g. 100→100), but silently wrong otherwise: with the default 3 plates × 80 wells × 100 µL × 1.1 excess, the reagent-needed cell showed "26 mL" in the Value column but "**30000** uL (incl. 10% excess)" in the Notes column for the same number — the true value is 26400 uL. Fixed by switching those specific calls to `Math.round()`, which is what the code actually meant; left every other `fmt(x, 2/3/4)` call alone since 2-4 significant figures reads correctly for their default inputs and wasn't the pattern that broke. **Lesson: `fmt(x, 0)` anywhere in this file is a latent version of this same bug — grep for it before reusing the pattern in a new calculator.**

**Mobile audit, Protocols absolute priority (2026-07-14, same day):** Jon asked to "re analyze everyhting para movil... prioridad absoluta protocols." **Tooling note**: this sandbox's `resize_window` (and CSS `zoom`) both hit a hard floor around 517px viewport width — could not get a literal true-phone (~375px) screenshot. Worked around it by (1) testing everything real at the achievable 517px width, and (2) for anything narrower, forcing a DOM element's own `width`/`max-width` via `element.style.cssText` to reproduce the exact box-model math a real narrow viewport would produce, then checking `scrollWidth > clientWidth` / `scrollHeight > clientHeight` for objective overflow detection (not just eyeballing) — since `element.style.width` doesn't change `window.innerWidth`, this method verifies layout math but does NOT itself trigger real `@media` breakpoints, so breakpoint CSS was written and then separately confirmed to be *correct as authored*, not observed firing live in-browser at true phone width. **Two real bugs found and fixed in `Protocols/protocols.html`**: (1) `header{height:58px}` (fixed height) combined with an unconstrained subtitle `<p>` — at narrow widths the subtitle wraps to 2 lines, needs more than 58px, and since `height` (not `min-height`) doesn't let a box grow, the wrapped text visually overflowed past the box and — because `header{position:sticky}` — permanently overlapped whatever content sat below it while scrolling. Confirmed via `scrollHeight` (57) baked at the *unwrapped* single-line height while the rendered content clearly needed more. Fixed by changing to `min-height:58px` (verified this alone stops the overlap — box now grows instead of clipping) plus, as compact-header polish, a new `@media(max-width:480px){header p{display:none}}` (keeps the header a tidy single line on real phones instead of just "less broken but still tall"). (2) The CRISPR Knockout/Knock-in `.delivery-bar` (label + native `<select>` in one `display:flex` row, no `flex-wrap`) — a `<select>` resists shrinking below its widest `<option>` text's intrinsic width by default (flex items have `min-width:auto`), so at a 343px-wide container (≈375px phone minus padding) the bar measured `scrollWidth:410` vs `clientWidth:341` — a genuine 69px horizontal overflow, confirmed by direct measurement before any fix existed. Fixed with `@media(max-width:480px){.delivery-bar{flex-wrap:wrap} .delivery-bar select{width:100%;max-width:none}}` — re-measured after the fix: `scrollWidth` dropped to exactly `clientWidth`, confirmed zero overflow, bar wraps to label-then-full-width-select instead. Also added in the same pass, lower-confidence/less rigorously verified but low-risk: `.proto-grid{grid-template-columns:1fr}` at ≤480px (mostly redundant with the existing `auto-fill,minmax(230px,1fr)` — belt-and-suspenders), `.proto-table{overflow-x:auto}` as a defensive wrapper (no actual overflowing table found in testing — none of the app's tables use `white-space:nowrap` or unbreakable tokens, so cell text already wraps safely), and a modest touch-target padding bump on `.dtab-btn`/`.btn`/`.btn-sm`/`.filter-pill` at ≤480px (not measured against a specific failure, just brought closer to the ~40-44px guideline). **Final verification**: zero `document.documentElement.scrollWidth > window.innerWidth` across home grid, a calculator protocol (all 3 tabs), a wide-table protocol (PyMOL), and a dense-content protocol (TR-FRET) at the achievable 517px width; zero console errors.

**Suite-wide pass, "arregla todos los bugs que conozcas" (same day):** Jon asked to fix everything flagged in the survey above. Correction to that survey first: the earlier "no subtitle `<p>`" read for 8 apps was a false negative — those apps use a `<div class="app-sub">` instead of `<p>` for the header subtitle, not no-subtitle-at-all, so the actual risk pool was all 12 other apps, not 2. Applied `height`→`min-height` on the `header{...}` rule (`Arc/arc.html`, `Beacon/beacon.html`, `Cryostorage/cryostorage.html`, `Cuppa/cuppa.html` (54px, not 58px), `Helix/helix.html`, `Labcyte_Echo/labcyte_echo.html`, `LDI/ldi.html`, `Lumina/lumina.html`, `Plate_Designer/plate_designer.html`, `Protein_Tools/protein_tools.html`, `Ribbon/ribbon.html`, `Spectra/spectra.html`) plus a `@media(max-width:480px){header .app-sub{display:none}}` (or `header p{display:none}` for the 4 apps using `<p>`) in each. **Before applying anything, checked whether each app already had protection further down the file** rather than assuming the Protocols bug generalized uniformly — it didn't: **9 of the 12** (Cryostorage/Iceberg, Cuppa, Helix, Labcyte_Echo/Echo, LDI, Plate_Designer/Blueprint, Protein_Tools, Ribbon, Spectra) already had their own `white-space:nowrap;overflow:hidden;text-overflow:ellipsis` treatment on the subtitle at `@media(max-width:720px)` plus a hide-entirely rule at `@media(max-width:420px)` from an earlier mobile-audit pass — meaning `height:58px` was never actually unsafe there (ellipsis prevents wrapping at any width below 720px, so the box never needed to grow). My added rules are redundant-but-harmless for those 9. **Only 3 apps had zero prior protection**: Arc, Beacon, Lumina. Verified via direct DOM measurement (force `header` to a 375px box with the *old* `height:58px`, compare `scrollHeight` vs `clientHeight`) rather than assuming: **Beacon genuinely broke** — its subtitle is 93 characters, the longest in the whole suite ("Gain-of-Signal & Tracer Displacement · Plate Map · Z′/Z · dose-response · tracer titration"), wrapped to 3 lines and overflowed past the fixed-height sticky header exactly like Protocols did; confirmed the fix resolves it (`scrollHeight` back to matching `clientHeight`). Arc (50 chars) and Lumina (47 chars) did NOT actually overflow even unprotected — short enough to fit — so these were hardened proactively rather than fixed for an active bug. **Net result: one more real, confirmed-broken app found and fixed (Beacon); two apps (Arc, Lumina) given the same protection defensively; nine apps got a harmless no-op.** Also checked the 3 remaining apps not in the original 12-app survey: **LabMate** has no fixed `height` on its header at all (`flex-direction:column`, auto height) — never had this bug, nothing to fix. **Dora/Degradation Explorer** has no `<header>` element in its markup at all (top bar is `.tabs`, not header+subtitle) — not applicable; its mobile-breakpoint `header{padding:0 12px}` rule is pre-existing dead CSS (targets a selector that matches nothing) but causes no harm, left alone. **Fabricata** has a header with no subtitle text (just logo + title + a "Beta" badge) — no wrap risk, not touched (inert easter egg, lowest priority). **`hub-shell.html`'s own `#hub-nav`** also uses fixed `height:58px`, but its title span already uses `text-overflow:ellipsis` (never wraps) and its search/settings controls are separately width-constrained — already correctly handled, not touched. Rebuilt `dHUB.html`, zero console errors on Beacon/Iceberg/Arc/Lumina spot-checks post-fix.

**Protocols: added calculators to 10 more protocols + fixed RNP electroporation (same day):** Jon: "hay que anadir calculadoras donde no las hay... en RNP electroporation hay calculos embedded en el protocolo, eso estaria mejor tenerlo per mL/tube/well... para que el researcher pueda ir a la TC hood sabiendo exactamente que anadir... re-check all protocols e identifica potenciales calculos." **RNP branches first (explicit ask)**: both CRISPR Knockout's and Knock-in's RNP delivery-method branches previously had a static `.no-calc-note` ("fixed-recipe... no calculator needed"); replaced with real per-electroporation scaling calculators (`calcKoRnp`/`calcKiRnp` + matching `generateXRnpSteps`) — KO scales the stated "12 uL R buffer + 1 uL gRNA + 1.2 uL Cas9 per 2 electroporations" down to a per-reaction rate and back up by N; KI scales RNP+ssODN volumes per electroporation and derives the underlying 1:1 Cas9:gRNA assembly volumes. Wired into `setMethodBranch()` so switching to RNP auto-computes immediately (no dead calculator, matches the "know exactly what to add" goal) and into the existing `_methodState`-aware `genSteps()` so the Output tab now generates real steps for RNP too (previously `null`/no-calc fallback). **Then a full re-audit of all 21 reference-only protocols** (read every one's actual embedded recipe text, not guessed) to find latent per-N-samples/per-well/per-tube math that was static text instead of a calculator. Found 10 real candidates, user chose to build all of them (not just the top tier): **Miniprep** (buffer volumes × N samples), **NucleoSpin** (NTI buffer = gel weight × ratio), **Immunoprecipitation** (antibody volume from protein-amount:antibody ratio + stock conc — reproduces the protocol's own "40 uL/mg" worked example exactly; separate bead/wash-buffer batch scaling), **HiBiT Blotting** (same dilution-cascade pattern as the pre-existing HiBiT Lytic calculator, scaled by total blot volume instead of wells), **Single Cell Sorting** (conditioned-media mix — reproduces the protocol's own "38+38+19=95 mL for 6 plates" example exactly), **Western Blot** (lysis buffer master mix by well count/format; also fixed a stale line claiming "BCA & Sample Prep calculator not yet migrated to this app" — it has been migrated since Pass 2, just never updated), **Retro/Lentiviral Transduction** (3-plasmid packaging mix by dish count + Polybrene dilution), **TR-FRET** (LANCE-format assay master mix, generic C1V1=C2V2 per-reagent dilution against a chosen final concentration and well count), **Fluorescence Polarization** (protein-tracer mix prepared at 2× final since it's diluted 1:1 with compound in-well), **SPR** (two dilution-series generators — serial compound series reproduces the protocol's own "1000, 333, 111, 37, 12, 4, 1.3 nM" example exactly; linear-step DMSO calibration curve). All 10 got a `hasCalc:false→true` flip, a genSteps() function, and were added to the `DOMContentLoaded` init block (all have meaningful defaults, so results render immediately on tab open rather than waiting for the first `oninput`, same UX as the RNP fix). **Caught and fixed one real bug via testing, not just eyeballing**: the Lentiviral Polybrene dilution formula (`(target * mediaVol) / (stock * 1000)`) had a spurious `*1000` — `stock` was already in mg/mL, which is numerically identical to ug/uL, so no conversion factor was needed at all; the bug meant every output was 1000× too small (showed "0.00 uL" instead of the correct 1.60 uL). Fixed to `(target * mediaVol) / stock`; re-verified against the protocol's own stated "1.6 uL/2 mL" example. **Verified every one of the 12 new/changed calculators** in a real Chrome tab against the source protocol text's own worked examples wherever one existed (3 exact reproductions: IP's 40 uL/mg, SCS's 95 mL, SPR's dilution series, Lentiviral's 1.6 uL Polybrene) — this is a stronger check than just "does the math look plausible," since it validates against numbers Jon's own lab protocol already committed to. Zero console errors. Protocols with calculators: 10 → 20 (of 31 total).

**Protocols: sticky notes system, both flavours (same day):** Jon: "podemos anadir un sistema de sticky notes para que el usuario anada cosas o notas a los protocolos... cuanto mas interactivo mejor." Asked which style first (AskUserQuestion) — Jon picked **both**: (1) free-floating pastel notes that drag anywhere over the viewport and persist per-protocol, and (2) a small pin icon after every Protocol-tab step that expands to an inline per-step note. No backend in this app, so everything is `localStorage`-backed under key `proto_notes_<pid>` = `{floating:[{id,x,y,w,h,color,text,tilt}], steps:{stepKey:text}}`. **Floating notes**: a `#sticky-layer` (`position:fixed`, full-viewport, `pointer-events:none` so it never blocks clicks on the page under it — individual notes re-enable `pointer-events:auto`) plus a `.notes-fab` "+" button (bottom-right, `position:fixed`) that only shows while a protocol is open (wired into `openProtocol`/`closeProtocol` via `openNotesForProtocol`/`closeNotesForProtocol`). Notes have a 6-colour pastel palette (yellow/pink/blue/green/orange/purple, deliberately fixed paper-colours that do NOT follow the app's dark/light theme — a real sticky note looks the same regardless of room lighting; text is a fixed dark grey `#2b2b2b` for exactly this reason), a drag handle header (mouse + touch), a per-note colour-dot picker, a delete button, and a `resize:both` textarea (native browser resize handle) for manual resizing. Since notes are `position:fixed` siblings of `#print-root` at the body level, the existing print rule (`body.printing > *:not(#print-root){display:none!important}`) already hides them from PDF export for free — verified, no new CSS needed. **Step-anchored notes**: `assignStepNoteKeys(pid)` runs once per protocol (lazy, on first open) and tags every `.proto-list li` in the Protocol tab with a stable `data-note-key` (`pid-s0`, `pid-s1`, …) in DOM order — this naturally also tags `<li>`s inside currently-hidden CRISPR delivery-method branches without any special-casing, since visibility is just `display:none` on an ancestor. Each `<li>` gets a small pencil-icon button (dims to accent colour when the step has a note) and an inline `.step-note-box` that toggles open on click. **Two real bugs found and fixed via interactive testing, not assumption**: (1) A data-loss race — `addStickyNote`/`deleteStickyNote`/`setStickyNoteColor` all called a full `renderStickyNotes()` (tears down and rebuilds every note from a fresh `localStorage` read), but the note-body's own save was debounced 300ms; clicking a colour swatch shortly after typing would read `localStorage` *before* the debounced text-save had committed, silently reverting the note to whatever text existed before typing. Fixed with a `_flushLiveNoteText()` helper that reads the *current* DOM textarea value of every rendered note straight into the data object before any of these three mutating operations — confirmed by reproducing the race, then confirming text survives a rapid type-then-recolour sequence. (2) Starting a drag exactly on a colour dot or the delete button also fired that control's own click (mousedown bubbles to the header's drag-start listener regardless of a child's `stopPropagation()` on a *different* event type) — fixed by gating `startStickyDrag` on `e.target === head` so only clicks on the header's own background (not its children) initiate a drag. Verified end-to-end in a real Chrome tab: add/type/drag/recolour/delete a floating note, add/persist a step note, reload confirms both persist and the step's pin icon shows its "has-note" state correctly, notes are correctly isolated per-protocol (opening a different pid shows a clean layer), dark mode looks correct, zero console errors.

**Protocols: icon redesign + sticky notes rework (same day, follow-up feedback):** Jon: "protocols icon css it's poor, optimize. also, we need a better name" — then, before that landed, a second message: "notes should appear in the output. las notas en cada paso haz que este desactivadas pero al darle al + de notas, da la option de a pie de paso o sticky note classic o sticky note moderna (estilo de la app, mas sobrio y profesional, flotante pero snap to lateral grids)." Handled in that order. **Icon**: the old icon (`<rect>` clipboard + a checkmark `<path>`) was a generic "todo list" glyph whose checkmark geometry looked muddled at 18-22px — replaced with a clean two-page open-book glyph (`M12 6.2c-1.6-1.1-4.2-1.6-6.8-1.1v12.4c2.6-.5 5.2 0 6.8 1.1c1.6-1.1 4.2-1.6 6.8-1.1V5.1...` + a center spine line) in both `Protocols/protocols.html`'s own header and `hub-shell.html`'s home-card logo, so they stay in sync (the nav breadcrumb icon auto-clones whichever SVG is in the home card, so it updates for free). **App rename is still pending** — asked via AskUserQuestion (Primer/Codex/Almanac/keep-Protocols), got no response within the 60s window; did not guess, left the name as "Protocols" and proceeded with everything else rather than blocking on a branding decision only Jon can make. Re-ask if it comes up again. **Notes rework** (the larger, second ask): (1) step-note pencil icons are now **off by default** — a new `body.step-notes-on` class (persisted in a separate `proto_notes_prefs` localStorage key, not per-protocol) gates their CSS visibility; (2) the "+" FAB is now a 3-item popup menu (`#notes-menu`): "Pin notes to steps" toggles the above pref, "Classic sticky note" adds the existing pastel draggable note, "Modern note" adds a new **sober/app-themed** note (`background:var(--surface)`, `border:1px solid var(--border)`, no colour-dot picker, `.modern-note`) that free-drags while held but **snaps into a left- or right-edge column stack on drop** — side chosen by which half of the viewport the drop x-coordinate lands in, slot chosen by comparing drop y against the vertical midpoint of each existing note in that column (`side`/`order` fields replace the classic notes' freeform `x`/`y`, so the whole column just recomputes `top = MODERN_TOP + index*(MODERN_H+MODERN_GAP)` from array order — no stored pixel position to go stale). New notes alternate left/right based on whichever column currently has fewer notes. (3) **Notes now appear in every export path**, not just live on the Protocol tab: `refreshExportPanel()` appends a `{label:'My notes', bullets:[...], isNotes:true}` entry (built by the new shared `buildNotesExportBullets(pid)`) onto whatever `genSteps()` returned — meaning a reference-only protocol with zero calculator now gets a real Output-tab card list instead of the generic "no calculator" fallback, the moment it has any notes. `exportProtocolPDF()` splits `_exportSteps[pid]` into calc-vs-notes by the `isNotes` flag so notes get their own "My notes" print heading instead of living under "Calculated values". `copyProtocolText()` appends the same bullets as a plain-text block. Notes text is user-typed free text (not the calculator functions' own trusted HTML-bearing bullets), so `isNotes` bullets get `esc()`'d specifically wherever they're written into innerHTML (Output tab render, PDF) while staying raw for the plain-text copy paths — a stray `<` or `&` in a note (e.g. "if OD<0.6...") no longer risks breaking the print layout. **One more real bug found via interactive testing**: editing a note while the Output tab was already the active view didn't update it — `refreshExportPanel()` only ever ran on tab-switch or explicit triggers, and nothing re-ran it after a note's *debounced* (300ms) save actually landed, so the visible "My notes" card could show stale content for up to 300ms or, worse, never update if the user didn't switch tabs afterward. Fixed with a shared `_refreshOutputIfActive(pid)` helper called from `updateStickyNoteText`, `deleteStickyNote`, and `saveStepNote` — confirmed by staying on the Output tab, typing into a floating note, and watching the "My notes" list update live without touching another tab. Verified: pin-icons hidden by default and toggle correctly, modern note added/dragged left→right and snapped cleanly, data model (`side`/`order`) correct after the drag, notes appear in Output for both a calculator protocol (Gibson) and a reference-only one (Transformation), dark mode correct for both note styles, zero console errors.

**Protocols: notes-in-output rework, round 2 (same day, follow-up feedback, still v1.3.7 — no version bump):** Jon: "notas en paso especifico mejor que se pueda editar al hover... en el output tiene que aparecer aqui esta nota, no al final. also, en el output no poner los iconos de editar cada paso. los calculos tienen que verse reflejados en el output." Three fixes plus a guardrail to re-verify. **(1) Hover-to-edit discovery**: step-note pencil icons (already gated behind the `step-notes-on` pref from round 1) now only reveal on `li:hover` within a pinned protocol, via `body.step-notes-on li:hover>.step-note-btn`; a `.has-note` override keeps a step's icon visible even unhovered once it actually has a note, so written notes stay discoverable on revisit instead of only showing on a lucky hover. **(2) Notes now render inline at their own step in every export, instead of collected into a trailing "My notes" block.** The old `refreshExportPanel`/`exportProtocolPDF`/`copyProtocolText` all concatenated a synthetic `{label:'My notes', isNotes:true}` step (built by `buildNotesExportBullets`, which extracted "Re: '&lt;step text&gt;' — &lt;note&gt;" bullets) onto the end of whatever `genSteps()` returned — structurally incapable of showing a note next to its actual step, since the note text was pulled out of the DOM rather than left where it was written. Replaced with one shared `buildCleanProtocolClone(pid, noteClass)`: clones the Protocol tab, removes every `.step-note-btn` (edit icon), and swaps each non-empty `.step-note-box` for a static read-only `<div>` **in place**, reading the *live* textarea value rather than a possibly-debounce-lagged `localStorage` snapshot (same race class already fixed for floating notes in round 1, now applied here too). One clone, three consumers: `exportProtocolPDF` uses its `innerHTML` directly for the print HTML (a `print-note-inline` class, fixed light-page colours, scoped under the existing `#print-root` `@media print` block); `copyProtocolText` mounts the clone off-screen (`position:fixed;left:-9999px`, forcing `display:block` since it inherits `.dpane{display:none}` whenever the Protocol tab isn't the one currently active) just long enough to read `.innerText` — a detached node has no layout, and `innerText` needs one; `renderExportPanel` (Output tab) appends the clone's `innerHTML` — themed via CSS vars (`export-note-inline`) so it's dark-mode-correct — as a new "Protocol steps · your notes" section **below the existing calculated-value cards**, gated on a new `protocolHasAnyNotes(pid)` check so a protocol with zero notes renders exactly as it did before this change (no added clutter). Floating (unanchored) notes genuinely have no step to sit next to, so they still collect into a small trailing "General notes" list inside that same section — the one place "at the end" is still correct, since there's no "here" for them. **(3) Re-verified the calculator guardrail, not just assumed it still worked**: `renderExportPanel` gained a `hasCalc` param (sourced from `PROTOCOLS[].hasCalc`) so its empty-state message can tell "calculator not yet run" apart from "this protocol has no calculator" now that the old `isNotes`-flag branching that used to distinguish them is gone; the calculated-value card rendering itself was untouched, and was re-confirmed live post-rework against Gibson Assembly (4 calc cards, correct PCR/DpnI/assembly volumes) and Western Blot (correct lysis-buffer master-mix values). Verified end-to-end in a real Chrome tab, reached via the app iframe's own `contentWindow` (dHUB embeds each app in an `<iframe>`, so the top frame's `document` can't see app internals — this tripped up the first verification attempt): hovering a step reveals only that step's icon; a typed note persists and shows up inline in the Protocol tab, the Output tab, the PDF-bound clone, and copy-as-text; a protocol with only a floating note gets the full protocol mirror plus a "General notes" line; a protocol with zero notes shows no extra section at all; classic and modern floating notes still add/drag/persist correctly (regression check); zero console errors. **One self-inflicted hazard for next time**: calling `exportProtocolPDF()` (or `window.print()`) directly from the JS console to test the print path opens a real native print dialog that blocks the tab and hangs CDP commands — verify the `#print-root`-bound HTML string directly instead, the way the final verification pass did, rather than actually invoking print.

**Protocols renamed to Papyrus (2026-07-14/15, same continuous session, still v1.3.7 — no version bump):** the app-rename question from earlier this session (Primer/Codex/Almanac/keep) had gone unanswered; Jon revisited it and asked for something Library-of-Alexandria-themed. Offered Alexandria/Athenaeum/Papyrus/Stacks, Jon picked **Papyrus**. Followed the same rename pattern already established for Echo/Dora/Blueprint: the internal id (`protocols`, used everywhere from `ALL_APP_IDS` to `localStorage` keys like `proto_notes_<pid>` to the iframe id `frame-protocols`) was **left untouched** — only user-facing display strings changed, to avoid a much bigger and riskier refactor for a cosmetic rename. Changed: `hub-shell.html`'s home card name, `APP_INFO.protocols.name` (`'Protocols'`→`'Papyrus'`, letter `'Pr'`→`'Pa'` since `'P'` was already Protein Tools'), the `New app:` changelog bullet, and the 4 `HUB_SEARCH_INDEX` entries that reference this app (`t:`/`app:` fields updated, `'protocols'` kept as an extra search keyword for continuity — same convention as the Echo/Dora/Blueprint renames). In `Protocols/protocols.html` itself: `<title>`, the page `<h1>`, and the PDF export footer text (`"dHUB Protocols · exported..."`→`"dHUB Papyrus · exported..."`). Left alone on purpose: the on-disk folder name `Protocols/` and the file name `protocols.html` (same git-mv-risk-for-no-payoff reasoning as the other app renames), the "Springer Nature Protocols" journal citation inside the SPR protocol's own tip text (a real journal name, not the app), and LabMate's generic use of the word "protocols" as a category/tab label (unrelated app, own vocabulary). Verified in a real Chrome tab: breadcrumb reads "dHUB › Papyrus", home card reads "Papyrus", the iframe's own `document.title` is "Papyrus", zero console errors.

**Papyrus: Gibson Assembly corrected to match Jon's real bench workflow (2026-07-15, same continuous session, still v1.3.7):** Jon: after PCR linearisation + DpnI, he purifies by agarose gel — loads the *entire* DpnI reaction (with 6× loading dye), band excision, then purifies with the NucleoSpin kit already in the app — not the PCR-cleanup-column workflow the protocol previously described. Updated in four places that all needed to agree: the Protocol tab's step-2 instructions (now: add 6× dye to the whole reaction → run on a 1% gel → excise → NucleoSpin gel extraction, dropping the old separate "purify with a PCR cleanup column" step and the now-redundant "optional gel check" bullet, since the real gel run already serves that purpose), the Calculate tab's DpnI calc-box note and its "Vector (post-PCR + DpnI + cleanup)" field label, and the `generateGibsonSteps()` text that feeds the Output tab/PDF/CSV. **Follow-up correction, same message thread**: Jon doesn't always PCR the insert — sometimes he already has it linearised with overhangs (ordered gene fragment, or PCR'd earlier). Asked that the summary/Output only include an "Insert PCR" step when the Insert PCR fields were actually filled in, not by default. `generateGibsonSteps()` now checks the *raw* (pre-fallback-placeholder) insert-PCR field values — name/conc/product-size/primers/Tms — and only pushes the `Step 1b — Insert PCR` entry when at least one is populated; the Vector PCR step's own label collapses from `Step 1a` to plain `Step 1` when there's no `1b` to distinguish it from, so there's never a numbering gap. DpnI stays `Step 2` and Assembly stays `Step 3` either way, since 1a/1b are subdivisions of one conceptual PCR step, not separate top-level steps — no downstream renumbering needed. Verified both branches live: filling in the insert-PCR name/conc fields produces `[1a, 1b, 2, 3]`; leaving them empty produces `[1, 2, 3]` with the insert step cleanly absent. Zero console errors.

**Papyrus: "Include calculated values" toggle, applies to all 20 calculator protocols (2026-07-15, same continuous session, still v1.3.7):** Jon: if he hasn't filled in a calculator, it doesn't make sense for the Output/PDF to include those values (they'd just show whatever placeholder defaults the calculator was seeded with, which reads as real data but isn't) — wanted a toggle so he can export just the plain protocol. Added a `_includeCalcs` map (pid → bool, default `true`) plus a checkbox rendered at the top of `renderExportPanel`'s output — but only when `hasCalc` is true, so reference-only protocols (which never had a calc section to toggle) show nothing extra. Unchecking it: (1) in the Output tab, replaces the calc cards with a one-line "Calculated values hidden — Output and PDF will show the plain protocol steps only" note, and (2) in `exportProtocolPDF`, skips the "Calculated values" heading/cards entirely. Both read the same `_includeCalcs[pid]` flag so they can't drift out of sync. **Deliberately did NOT touch CSV export** — `exportProtocolCSV` is *only* ever about calculated values (there's no "plain protocol" content in a CSV to fall back to), so the toggle would have nothing meaningful to do there; it stays exactly as before, driven by `_lastCalcTable[pid]`. The toggle's `onchange` calls `refreshExportPanel(pid)` to re-render immediately — works correctly even though `renderExportPanel` itself is also called directly by ~19 other calculator-specific functions (`renderGibsonExport` etc.) on every Calculate-tab keystroke, since the checkbox's state lives in the module-level `_includeCalcs` map, not in the DOM node that gets replaced on every re-render. Verified live: Gibson's toggle defaults checked with cards showing; unchecking hides the cards and shows the message; re-checking restores them; a reference-only protocol (Transformation) shows no toggle at all and its existing "no calculator" message is untouched; zero console errors.

**Papyrus renamed to Archive, folder + file moved this time (2026-07-15, same continuous session, still v1.3.7):** revisited the naming question again — Jon picked "Archive" from an earlier candidate list, but that collides with the existing **Arc** app (storyboard editor): "Archive" literally starts with "Arc", risking mix-ups on the grid/in search/spoken aloud. Flagged it via AskUserQuestion before renaming; Jon's answer: keep Archive, and **hide Arc** since he doesn't use it — see below. Unlike the Papyrus rename (display strings only), Jon explicitly asked to "modify corresponding names and directories" this time, so the on-disk path moved too: `git mv Protocols Archive && git mv Archive/protocols.html Archive/archive.html`, and `embed.py`'s `APPS` list updated to `('protocols', 'Archive/archive.html')`. The internal app **id stays `protocols`** — deliberately not renamed, same reasoning as every other app rename this session (touches `ALL_APP_IDS`, `localStorage` keys like `proto_notes_<pid>`, the iframe id `frame-protocols`; renaming it is a much bigger, no-payoff refactor for a cosmetic change) — so there's now a folder/id mismatch (`Archive/archive.html` internally keyed as `protocols`), which is fine, several apps already don't have id-name parity (`pd`=Blueprint, `deg`=Dora). Display strings updated in both `hub-shell.html` (home card, `APP_INFO.protocols.name`/letter `Pa`→`Ar`, changelog bullet, 4 `HUB_SEARCH_INDEX` entries) and `Archive/archive.html` itself (`<title>`, `<h1>`, PDF export footer, one code comment). Verified live: breadcrumb/home-card/iframe-document-title all read "Archive", app loads correctly from the new path, zero console errors.

**Arc hidden from the suite (2026-07-15, same message thread):** Jon: "Arc is useless, hide it" (asked twice). Investigated the hide mechanisms available before picking one: (1) the Firebase-backed `labConfig` admin toggle (`.lab-toggle` checkboxes → `/labconfig` RTDB write) looked like the "proper" built-in mechanism, but **admins bypass it entirely** (`labHide = (!isAdmin || _labPreview) && labConfig[id]===false` — Jon, always browsing as admin, would still see the card), and it requires a live production Firebase write from an authenticated admin session, which isn't something to do from a code-editing pass. (2) Simply removing `'arc'` from the `PACKAGES` grid-grouping array does NOT work either — `_buildPackages()` has an explicit "orphan card" safety net (comment: *"any app id not listed here falls into a trailing 'More' section so nothing ever silently disappears"*) — a card with no package assignment doesn't vanish, it just moves to a catch-all "More" section. **What actually hides it**: removed Arc's `<div class="card" data-app-id="arc">` markup block entirely from the home-grid HTML (so `_buildPackages()`'s `querySelectorAll('.card[data-app-id]')` never finds it — no card, no orphan, nothing to relocate to "More"), plus removed `'arc'` from `PACKAGES` (Design & Presentation), `ALL_APP_IDS`, and `APP_UNLOCK_WORDS` (its `storyboard` unlock code no longer does anything), and dropped its `HUB_SEARCH_INDEX` entry. **Deliberately left untouched**: `APP_INFO.arc` (harmless passive lookup, not referenced by anything that renders now), `APP_B64_NEW.arc`'s placeholder and `embed.py`'s `('arc', 'Arc/arc.html')` entry (so the build keeps embedding it — no build breakage), and the `#view-arc`/`#frame-arc` iframe markup (still present, so `openApp('arc')` would still technically work if ever called directly). Net effect: **fully hidden, zero data loss, fully reversible** — re-adding the card block + the three list entries brings it straight back; nothing was deleted. Verified: `ALL_APP_IDS` no longer contains `arc`, no `[data-app-id="arc"]` element exists anywhere in the built page, `embed.py` still builds clean (still embeds Arc's file, just unreachable through normal UI), zero console errors.

**Echo Data Analysis: full visual/functional audit with real test data (2026-07-15, same continuous session, still v1.3.7):** Jon: "carga los datos de muestra y comprueba en la web mejoras visuales... se thorough... optimiza. sugiere mejoras o nueva implementaciones." Loaded the app's own built-in `loadTestData()` bundle (Echo CSV + 6 PHERAstar plates, 63 rows = 21 real compounds × 3 protein targets BRD2/BRD3/BRD4) and walked every tab — Console, Plots (Scatter/Box/Selectivity), Curves (Single/Compare), Plate (all 5 colour-by modes), Properties, Results, History, Protocol, Source Plate, Guide — plus a light-mode check and a narrow-viewport (900px) pass. Found and fixed **4 real bugs**: (1) **Box plot Y-axis went negative** (`-45.7 nM`) for DC50/AbsDC50 — physically impossible for a concentration; the auto-scale padding in `renderBoxPlotVar()` had no floor. Fixed by clamping the auto-computed `ylo` to 0 specifically for `DC50_nM`/`AbsDC50_nM` (never for LogIC50/pDC50/Hill/R², which can legitimately be negative), and only when the user hasn't typed an explicit Y-min override. (2) **Compare-mode compound chips and chart legend were ambiguous**: the same Sample ID appears 3× (once per protein target) with **zero visual distinction** — clicking one "EDA-013" chip highlighted all three identically-labelled entries, and the canvas legend read "EDA-013 — EDA-013 — EDA-013". The comparison TABLE below already showed a Group column correctly, and Single-mode's own dropdown already disambiguates via "EDA-013 (BRD2)" — the chips/legend just hadn't been updated to match. Added the protein suffix to both (`buildCompareList()`'s chip label, `drawMultiCurve()`'s canvas legend `lbl`), matching the established convention exactly. Selection itself was already correct underneath (verified: 3 separate curves DID render, just unlabelled) — this was a display-only fix. (3) **Flag-status Plate colouring was completely broken for single-assay-type runs** (the default/common case) — every well rendered as generic grey regardless of actual QC flag, never red, never the correct green. Root cause: `_plateFlagMap` is built keyed by the real assay type string (e.g. `"hibit"`), but the lookup's `_bcAssay` fallback for "no multi-assay config" resolves to the literal string `'_'`, which never matches — a pure key-mismatch bug, confirmed by inspecting `window._plateFlagMap` directly (`{hibit: "No"}` vs. looking up `['_']`). Fixed by falling back to the sole value present in the per-compound flag map when the assay-specific key misses (`Object.values(_flags)[0]`), which is always correct for single-assay runs and doesn't touch the already-working multi-assay path. (4) **At narrow viewports (~900px), the fixed "Setup" button completely covered the "Guide" tab** — confirmed via `elementFromPoint` that clicks on Guide's screen position landed on the Setup button instead, because `.setup-reopen-btn` was `position:sticky;right:0` *inside* the same scrolling flex row as the tabs, so it always re-anchored over whatever tab happened to be at the trailing edge. Restructured so the 10 `.tab` divs live in a new inner `.tabs-scroll` (`flex:1;overflow-x:auto`), with Setup as a true sibling outside it (plain `position:static`, `margin-left:12px`) — Setup is now always fully visible in its own reserved slot, and the tab strip scrolls independently underneath without ever overlapping it (verified: scrolling `.tabs-scroll` now cleanly reveals Guide, clickable, no overlap; full desktop width unaffected — Setup sits with its normal 12px gap after Source Plate, matching the original visual spacing). **Two things flagged as suggestions, not silently changed** (real design decisions, not mechanical bugs): the Properties tab shows only **one** DC50 per compound (silently the last-processed protein-group row, no group label) for a dataset that's a genuine multi-target screen — needs a decision on how to surface 3 values per compound, not a one-line fix; and the Guide documents a **"Raw signal QC"** Plate-colouring mode (robust z-score/MAD-based well-outlier detection, marked "New.") and a **"DC50/Dmax (fitted)"** mode that **don't exist anywhere in the actual code** — either stale docs for a cut feature or a genuinely half-shipped one, and "Raw signal QC" in particular sounds like a legitimately valuable feature worth actually building if Jon wants it. Also verified (not a bug): light-mode toggle applies correctly at the CSS/computed-style level (`--bg`/`--surface` variables flip correctly) — screenshots taken during a light-mode check still *rendered* dark, which is very likely a browser/OS-level forced-dark-mode artifact in this sandbox rather than an app bug, since computed styles were unambiguously correct. Zero console errors throughout. Only `Labcyte_Echo/labcyte_echo.html` was touched — no other app.

**Blueprint: Gel Designer visual redesign (2026-07-15, same continuous session, still v1.3.7):** Jon: "el gel designer esta muy feo... dame un diseno mas limpio y checkea por la web, arregla errores visuales y ponlo bonito." Opened it live and compared against Blueprint's OWN other tab, Plate Designer, which is visibly more polished — used that as the reference design language rather than inventing a new one. Root visual problem: `.gd-preview-wrap` unconditionally rendered a checkered "transparency" pattern (`repeating-conic-gradient`) at `flex:1` width regardless of whether "Transparent bg" was actually checked — since the gel itself defaults to an opaque white background, this meant a small white gel graphic sat inside a huge, mostly-empty, busy checkered void that stretched to fill the whole preview pane. That's the main source of the "feo" impression. **Fixes**: (1) `.gd-preview-wrap` is now `display:inline-flex` (hugs its actual content size, like Plate Designer's own `.plate-canvas-wrap`) with a plain `var(--surface)` card background + border + shadow by default; the checkered pattern only appears via a new `.gd-transparent-bg` class that `gdDraw()` now toggles in sync with the "Transparent bg" checkbox's actual state on every redraw — so the preview only shows checkering when it's meaningful (i.e. actually previewing a transparent export). The plain-card default also reads nicely as a "gel on a lightbox" look against the app's dark theme. (2) **Real bug**: the rotated "MW" label sat almost touching the well's top edge — traced to a local `GAP=4` constant used as the text-anchor offset in `gdDraw()`'s label-rendering branch (separate from the `GAP_EXT` constant used only for space *reservation*, which was NOT the actual visual gap — bumping reservation alone didn't fix the visible crowding). Increased the actual render-time `GAP` 4→7, plus a smaller `GAP_EXT` 6→9 / min-clamp 30→34 bump for reserved space consistency. (3) **Real bug, narrow-viewport**: at ≤700px the stacked-column mobile layout starved the Preview pane down to ~24px of visible height (confirmed via `scrollHeight`/`clientHeight`: preview clientHeight was 24px against 178px of actual content) — `.gd-ctrl` and `.gd-preview` were both fighting over a fixed-height `.gd-layout` via `flex:1` + independent `overflow-y:auto`, appropriate for the wide 2-pane desktop layout but wrong once stacked. Fixed the `@media(max-width:700px)` block so `.gd-layout` scrolls as a single natural-height page (`overflow-y:auto`) and both children become `flex:0 0 auto` with `overflow:visible` — verified via computed styles (`scrollHeight === clientHeight` on both children post-fix) and a screenshot showing Preview fully visible after scrolling. **Polish** (not bug fixes, general "make it pretty"): reworked `.gd-sect-lbl` section headers with a top border-divider + more vertical rhythm; added a focus ring (`box-shadow`) to `.gd-select`/`.gd-input` matching the accent colour; rewrote `gdRenderWells()` to use new `.gd-wtype-select`/`.gd-wlabel-input` classes instead of inline styles (same behaviour, cleaner code, bordered/rounded table now instead of borderless); rewrote `gdRenderBrackets()` so each bracket is its own bordered card row (`.gd-brack-row`) instead of a bare flex row, and the delete "×" button now gets a red hover state (`.gd-brack-del`) instead of a static grey square. Verified live in Chrome across: default 20-well, a 26-well/M-size/titled/2-bracket case (card resizes gracefully, nothing overflows), light mode (card→white, same low-contrast-on-white pattern Plate Designer's own canvas already uses, so it's consistent not a new problem), dark mode, and the 680px mobile breakpoint. Zero console errors. Only `Plate_Designer/plate_designer.html` touched, only the Gel Designer panel/functions within it — Plate Designer itself untouched.

**Gel Designer: two more real label-overlap bugs, follow-up to the redesign (2026-07-15, same continuous session, still v1.3.7):** Jon: "sigue sin poner bien el lane text... esta pisando el gel" (still not placing the lane text right, it's stepping on the gel) — the MW-label gap fix from the redesign pass wasn't the whole story. Reproduced with real sample labels instead of the empty default (which had hidden this): **(1)** the reserved space for rotated "above/below" labels (`EXT_H`) was computed from a rough `charCount × fontSize × 0.62` estimate rather than actual rendered width — fine for a 2-character "MW" but for longer text like "Sample 1" or a real compound name, the estimate under-shot the true glyph width and the label visibly ran into the well row. Replaced the estimate with a real `ctx.measureText()` measurement (grabbing the canvas context early, before it's sized/cleared, purely to measure) and removed the old hard 130px cap on reserved height (canvas already auto-grows to fit content — Jon's own earlier 26-well/M-size test proved that — so there was no reason to cap it and truncate space for long labels). **(2)** A real direction bug, found while stress-testing the fix: with **Label position = Below well** and **Orientation = Angled −45°**, the label text climbed *upward* off the anchor and overlapped the wells/lane numbers instead of hanging downward and away — Vertical (−90°) "below" already worked correctly, isolating the bug to one specific `textAlign` branch: `ctx.textAlign = orient === -90 ? 'right' : 'left'` correctly right-aligned for −90° but wrongly left-aligned for −45°, which (given canvas's rotation-matrix sign convention) sends the text in the wrong direction for any non-vertical rotation. Fixed by using `'right'` unconditionally for the below+rotated case — derived the correct alignment via the rotation matrix, then confirmed against all 4 realistic combinations (above/below × vertical/angled) plus a genuinely long label ("Compound-XYZ-123 (50 nM)") to be sure the fix generalizes, not just patches the one repro. Zero console errors. Same file as the redesign pass (`Plate_Designer/plate_designer.html`), no other app touched.

**Echo: Selectivity plot axis-tick overlap fixed (2026-07-15, same continuous session, still v1.3.7):** Jon sent a screenshot — cramped/overlapping tick labels at both ends of both axes on the Plots → Selectivity chart, "this keeps happening in all plots for me, when i open it." Root cause: `buildSelectivityChart()` (`Labcyte_Echo/labcyte_echo.html`) set the Chart.js scale `min`/`max` directly to the raw 10%-padded data range (e.g. `6.6365`/`8.9465`) — non-round numbers. Chart.js still generates its own "nice" step-based ticks (0.5 here) independently of that boundary, so whenever the boundary didn't land exactly on a nice-step multiple, it got added as an *extra* tick very close to its neighbour (confirmed via `selChart.scales.y.ticks`: `[6.6365, 7, 7.5, 8, 8.5, 8.9465]` — a 0.36–0.45 gap at each end vs. the regular 0.5 step), reading as cramped/doubled labels, and — since that boundary tick sits flush at the chart edge — its label also risked clipping against `#sel-wrap`'s `overflow:hidden`. This is unconditional in `buildSelectivityChart()` (no user override), so it reproduced on literally every Selectivity view, matching "every time I open it." Reproduced live first (Chrome, `loadTestData()`), confirmed the exact tick values via `selChart.scales`, then fixed: new `_niceAxisStep(range)` helper rounds the axis bounds outward to a 1/2/5×10ⁿ step before assigning `min`/`max`, so the boundary always coincides with the regular tick sequence instead of adding a stray near-duplicate. Verified across three very different scales (pDC50 → 0.5 step, Dmax % → 10 step, R² → 0.02 step) — clean evenly-spaced ticks in every case, re-verified inside the actual embedded `dHUB.html` (not just standalone), zero console errors. Scatter/Box plots already only set explicit axis min/max when the user types a manual override (`Number.isFinite(xMinV) ? {min:xMinV} : {}`), so they weren't exhibiting this on default view — Selectivity was the one chart doing it unconditionally. Only `Labcyte_Echo/labcyte_echo.html` touched.

**Spectra rebuilt into BCA — real plate-based protein-assay workflow (2026-07-15, same continuous session, still v1.3.7):** Jon: the BCA tooling in the suite doesn't "really work" — Archive's "BCA & Sample Prep" calculator only ever took a manually-typed concentration, it never actually reads a plate or fits a curve; he does the real analysis by hand in Excel. Suggested repurposing Spectra (already had a CSV plate importer + linear/quadratic fit engine) into a dedicated BCA app. Reverse-engineered his real workflow from a live example file (`BCA20260205.xlsx`, PHERAstar 96-well export, not committed to the repo) before building anything: a 6-point BSA dilution series in technical duplicate (columns 1–2, rows A–F, 2/1/0.5/0.25/0.125/0 µg/µL) with manual exclusion of bad reads (a detector "overflow" well) before averaging and fitting; samples also in duplicate, converted via the inverse fit; a `Lysate preparation` sheet doing target-concentration/volume/water math that has no plate-analysis step feeding into it today — the "user says what dilution they did" correction step didn't exist anywhere and is the main reason the tool didn't really work for him. Dropped the old A280/Ratios/Std-Curve/Plate-Reader/Guide tab set (Protein Tools already has a more rigorous sequence-based A280 calculator; Jon didn't respond to a clarifying question on this within the ask window, proceeded with the documented recommendation) in favour of **Import → Standards → Samples → Normalize → Guide**. Ported/adapted rather than rebuilt: Beacon's PHERAstar `.xlsx` SheetJS import (copied its bundled SheetJS `<script>` block byte-for-byte — this exact copy is already verified safe inside dHUB's base64/`srcdoc` embedding, per an earlier Beacon embed-corruption fix) and its drag-paint plate-map painter (`pmWellDown/Over/Up`, adapted to two implicit roles — standards vs. samples — instead of Beacon's role-picker, since each tab only ever paints one role); Spectra's own `linearRegression`/`quadraticRegression`/`scPredictY`/`scInvertX` fit engine, repointing `scInvertX`'s quadratic-root selection from the now-gone `scRows` global onto `state.plateMap.standardGroups` (flagged in planning as the single highest-risk transcription bug in the port — a silently-wrong physical root, not a crash). New: per-well QC exclude/include (plain click, not drag, on an already-owned well — toggles it out of its group's average, stays visibly assigned but dimmed with a ✕, mirrors exactly what Jon does by hand for an overflow/outlier well), per-sample dilution factor (blank silently defaults to 1× but renders as "1× (unset)" rather than a plain "1", so an unconfirmed default never looks identical to a value actually checked), and the Normalize tab's `Protein(µL) = targetConc×targetVol/finalConc` / `Water(µL) = targetVol − Protein(µL)` math with an explicit "too dilute" flag (max achievable = the sample used neat) instead of ever showing a negative water volume. **Real bug found during the first import attempt**: the initial port assumed Beacon's flat per-well "Well ID per row" sheet shape (`Table End point`), but the real PHERAstar file's `Microplate End point` sheet — which the sheet-name regex matches first — is a *grid* layout (a header row of column numbers 1–12, then one data row per plate row) that Beacon never needed to parse since its own donor+acceptor dual-channel reads don't fit in one grid cell. `parseBcaTableEndpoint` rewritten to detect the numeric 1–12 header-row sequence and its column offset first, falling back to the flat per-well shape only if that's absent — verified against the real file (`state.plate` matched the sheet's raw values exactly, including the overflow well parsing as `0`). **Second real bug found via interactive testing**: the QC-exclude toggle updated the plate grid and recomputed the fit/table correctly but didn't refresh the chip list's "(N excluded)" annotation — `pmWellUp()`'s single-well-click branch was missing a `renderGroupList()` call; fixed and reverified. End-to-end verified against the real file in a real Chrome tab (not just Playwright): imported plate matched raw sheet values exactly; painting the real standard layout and excluding the overflow well raised R² from 0.894 → 0.986 (linear) / 0.999 (quadratic), matching the improvement Jon's own manual exclusion achieves; painted all 24 real samples, averaged absorbances matched his own spreadsheet's "Averages" table exactly; the Normalize formula reproduced his real spreadsheet's numbers bit-for-bit (`finalConc=1.9051679999999998, targetConc=1.5, targetVol=120` → `Protein=94.4798568945101, Water=25.520143105489893`, exact floating-point match). Cross-tab well-locking (can't paint a well the other tab already owns) verified. Zero console errors throughout, in both the standalone file and the rebuilt embedded `dHUB.html`. **Renamed app-wide**: `hub-shell.html` home card (name/description/tags/icon), `APP_INFO.spectra` (`name:'BCA'`, `letter:'S'→'BC'` since `'B'` is already Beacon's), 4 `HUB_SEARCH_INDEX` entries rewritten to the new tab set (kept `spectra` as a search keyword for continuity, matching every prior app rename in this repo). **Left unchanged on purpose, matching this repo's established rename convention** (Echo/Dora/Blueprint precedent — only Archive's rename moved the folder, because Jon explicitly asked for that one): the internal id `spectra`, the on-disk folder `Spectra/` and filename `spectra.html`, `embed.py`'s existing `('spectra','Spectra/spectra.html')` entry (no change needed). Archive's own "BCA & Sample Prep" calculator is untouched — it's a separate, later, gel-loading-buffer step (LDS/DTT volumes from a manually-typed concentration), not general water-dilution normalization, genuinely out of scope for this app.

**BCA: test data + visual audit, same-day follow-up (2026-07-15, still v1.3.7):** Jon: "load test data and optimize visually using web." BCA had no built-in demo dataset yet (every other data-heavy app in the suite — Echo, Beacon, Lumina — has one via a `⚗ Load test data` button); added `loadTestData()` generating a synthetic-but-realistic 96-well plate (deterministic pseudo-random jitter, not `Math.random()`, so reloading the demo is reproducible) — a 6-point BSA duplicate-pair standard curve following a genuine quadratic BCA response shape, 24 duplicate-pair samples, a handful of samples pre-set with confirmed dilution factors (others left unset on purpose to demo the "(unset)" badge), and **one deliberately bad replicate well** on the top standard (mimicking a real detector-overflow read) so the QC-exclude feature has something real to demonstrate immediately on load rather than requiring a real file to show its value. Then did a real interactive visual audit (not just a read-through) — all 5 tabs, light/dark theme, and the narrowest viewport this sandbox's `resize_window` can reach (~514px; true 375px isn't achievable here, a known environment limit noted elsewhere in this doc). Found and fixed **two real bugs**: (1) `switchTab()` had no re-render branch for `'import'` — the plate heatmap canvas is only ever drawn once at import time, so returning to the Import tab after toggling the theme (or after time on another tab) showed a stale-themed canvas; added `if(name==='import' && state.plate){ setTimeout(drawHeatmap,50); }` matching the pattern already used for the other three data tabs. (2) `.guide-code` (the Guide tab's formula blocks) used `white-space:pre` with no overflow handling — at narrow widths the longer formula lines (e.g. `Protein (µL) = target concentration × target volume ÷ final concentration`) silently overflowed their own box with no way to read the cut-off portion; added `overflow-x:auto` so it scrolls within its own box instead (this exact `.guide-code` rule was inherited unchanged from the original Spectra file, so the bug likely predates this session's rebuild — worth checking other apps' `.guide-code` blocks if this class of bug is ever reported elsewhere). One thing investigated and ruled out as a false alarm: the heatmap's low-absorbance wells render near-white by design (light→teal gradient, intentionally theme-independent) — initially looked like a stale-white canvas bug in dark mode until a direct canvas pixel read (`getImageData`) confirmed the actual background fill was the correct dark surface color; don't re-flag "heatmap looks washed out in dark mode" without checking pixel data first. Zero console errors, re-verified in both the standalone file and the rebuilt embedded `dHUB.html`.

**Known transitional state, not yet cleaned up:** none of the 31 migrated protocols' content has been removed from LabMate yet (Jon said to leave LabMate's non-Quick-Calc sections alone for now too, pending this review) — so `HUB_SEARCH_INDEX` still has some overlapping entries between LabMate and Protocols for the same topics. **Next steps, only after Jon reviews and approves:** remove the migrated sections from LabMate (Mol Biology/CRISPR/Cell Biology-minus-Cell-Lines/Proteomics/Genomics/Struct-Bio/Biophysics), dedupe the search index, decide what happens to LabMate's own Quick Calc section long-term ("ya veremos"), then treat the whole thing as a normal version bump + changelog entry + merge to `main` + push. **To revert entirely:** `git checkout main` — nothing on `main` has been touched at any point in this branch's history.

Previous session: 2026-07-13 (Suite-wide audit — Wave 1 correctness + shared fit-engine reconciliation; v1.3.6)
Hub apps: 15. Version v1.3.6. (All of this day's work is one version bump / one changelog entry — see the versioning rule.)
**Shared curve-fit engine reconciled (Echo canonical).** Jon chose to consolidate the drifted 4PL engines. Measured the drift first: `_4plVal4`/`_gain` identical everywhere, but `_lmFit` differed in all three and `_4plJac4_gain`/`_matInv`/`_solveLin`/`_xAtYMid`/`_fitBest` in some. Analysed every diff: **Lumina's were pure ES6→ES5 rewrites (identical math); Beacon's `_lmFit`/`_4plJac4_gain` were cosmetic (comments/formatting); Beacon's only real algorithmic difference was `_xAtYMid`** (nearest-neighbour vs Echo's interpolation — feeds 1 of 6 multi-start seeds). Verified BEFORE touching files: instrumented Beacon's `_fitBest` in-browser, replayed both test datasets with Echo's `_xAtYMid` → **maxParamDiff=0**. Then reconciled via new **`sync_fit_engine.py`** (copies Echo's canonical functions into Beacon/Lumina by brace-matching), `check_shared.py` now reports no drift. Re-verified: Beacon fits in-browser give the exact intended test values (logEC50=2→100 nM, R²=1); Lumina old-vs-new engine A/B'd in Node on fixed falling+gain data → **maxParamDiff=0.000e+0, r2Diff=0**. **Net: single canonical engine, zero change to any fit output.** Also this day: Degradation Explorer CSV formula-injection guard (`csvSafe`) + guide quadrant-label fix; **`check_shared.py`** drift monitor added. Workflow going forward: edit fit math in Echo only → `sync_fit_engine.py` → verify → `embed.py` (see Shared curve-fit engine section above).
**Spectra nonlinear standard curve (first Wave-3 feature, same day):** added a Linear/Quadratic **Fit** selector to the Std Curve tab. `quadraticRegression` fits `y=ax²+bx+c` via 3×3 normal equations (`_solve3`); a `scPredictY`/`scInvertX` abstraction replaces the old inline `(a-b)/m` so BOTH the Unknowns table and the Plate-Reader absorbance→concentration conversion follow the chosen fit. `scInvertX` solves the quadratic and picks the physical root nearest the standards' concentration range. Renamed `scLinear`→`scFit` ({type,...}). Verified in-browser: linear default R²=0.9997 unchanged; on nonlinear BCA-like data linear R²=0.911 → quadratic R²=0.996; inversion round-trips exactly (x=600→A=0.926→x=600.000). BCA/Bradford are genuinely nonlinear, so this removes interpolation bias.

**Wave 1 (correctness pass — same day):** Full audit of all 15 apps + Hub shell + `embed.py` (three domain-grouped Explore passes; findings + roadmap in `~/.claude/plans/warm-sparking-castle.md`). Decisions with Jon: **keep all apps** (no merges; Fabricata stays an inert easter egg), **de-dupe shared code with Echo as the canonical fit engine** (future wave). **Wave 1 shipped (this session):** (1) **Two 1000× scientific bugs** — A280→mg/mL was overstated ×1000 in **Protein Tools** (`:1338`, `cR*mw*1e3`→`cR*mw`) and **Spectra** (`:890`/`:907`, dropped the stray `*1000`; Guide text `:687` fixed). µM was always right. Verified in-browser: Spectra 0.85 mg/mL (was 850), Protein Tools 0.79 mg/mL for 77.8 µM/~10 kDa. (2) **Spectra**: high-DPI canvases (`sc-canvas`, `plate-canvas`) grew on every redraw because they read `offsetHeight` with no CSS height — pinned CSS `height`/`width` (verified sc-canvas stays 300 across redraws); labelled-plate CSV that starts with row "A" no longer dropped (fixed `firstIsHeader` to not treat an A–H row label as a header, `:1367`); guarded the `localStorage` theme read; heatmap teal `#26a69a`→Nature `#51c3ce`. (3) **esc() hardening** — upgraded to full `&<>"'` in Helix/Lumina/LDI/Degradation/Lab Designer; added an `escJs()` (backslash-escapes for a single-quoted JS string in an inline `on*` handler — HTML-escaping the quote is NOT enough there, the parser decodes it back) and used it for the Lumina sample chip + Degradation compound onclick; Helix `parseFASTA` now strips to `[A-Z]` at the source (kills injection into the ORF map / copy button). Wrapped Degradation's compound/target display in `esc()`. (4) **Iceberg**: `maybeSeedRealData` read `window.parent.isAdmin` synchronously before Firebase resolved → blank inventory; now `seedWhenAdminReady()` retries ~4 s and re-renders on late seed. (5) **Echo**: the rect-select `document` keydown is now bound once (`_attachRectListeners._escBound`) so it can't stack if the overlay is recreated. (6) **Hub**: `arc`/`ribbon` were missing from `ALL_APP_IDS`/`APP_UNLOCK_WORDS`/`HUB_SEARCH_INDEX` (unlock words `cartoon`/`storyboard`; search entries added) — they were always-visible and un-hideable; now consistent. Purged dead search deep-links to removed LabMate **Reference** + **PROTAC** sections; re-pointed **Lab Timers**/**Experiment Planner** to their real `sec-timer`/`sec-planner` and added a `_navToSection` fallback in LabMate's `switchTab` message handler for sections that have no top-nav button. (7) **embed.py** now guards missing source files and asserts exactly one replacement per key (a typo'd key used to ship a blank app silently); deleted empty `Manual_Plate_Reader/` and `dist/`.
**Audit correction — LabMate "2 MB image strip" was a MISREAD and was NOT done:** the ~2 MB of inline images are *legitimate content*, not dead Pip variants — the 6 `.pip-*` PNGs + two JS-assigned PNGs + the header PNG drive the **pip-story** section, and the 9 JPEGs are **cell-line photos** (`cell-card-img`, used by the Cell Lines feature). Stripping them would break real features. `Labmate/RDKit_minimal.js`/`.wasm` are orphaned (0 refs) but **gitignored**, so they're already out of the repo and the built Hub — deleting them has zero product impact (CLAUDE.md RDKit notes updated to say so). PROTAC/Reference dead code (~37 scattered residue refs) left for a later careful pass — cosmetic, risky to bulk-excise in an 11k-line file. Real remaining LabMate size win would be image *recompression* or lazy-loading pip-story, not deletion.

Previous session: 2026-07-12 (Cuppa refresh — last-updated bar, banter, Fraunces, fun-stats polish, admin-gated shame button; v1.3.5)
Hub apps: 15. Version v1.3.5. (One version bump / one changelog entry for the whole day.)
**Admin-gated shame button:** the Wall-of-Shame "Name & shame on Slack" button (`copyWallOfShame()`) is now **admin-only**. Jon wanted the wall kept (it's an established dynamic) but only he should be able to fire the Slack call-out. Added an `if(!isAdmin){ _snitchBlock(); return; }` guard at the top of `copyWallOfShame()`; `_snitchBlock()` shows a custom (non-native) centred pop-up (`#snitch-pop`, built lazily, tap-to-dismiss + 3.2s auto-hide) with a **rotating** playful message from `_SNITCH_MSGS` ("We don't snitch here.", "Put the megaphone down — this one's Jon's job.", etc. — avoids immediate repeats). Admin path unchanged (still copies the Slack message). Verified in-browser as a viewer (`isAdmin=false` → pop-up, no copy).
**Consolidated top bar + banter + fun-stats polish:** three follow-on Cuppa changes. (1) **Consolidated the top bar**: Jon flagged an "excess of banners" when embedded — The Hub's breadcrumb (fine), then *three* Cuppa-level rows (an almost-empty `viewer / Sign in / Settings` header row, the last-updated bar, the tab strip). Moved Cuppa's own controls (`.hdr-right`: role badge, Sign in, Settings, opts gear) out of `<header>` and into `.lastupd-bar`, so the header now holds only branding. Because the controls no longer live in the header, `hub-shell.html`'s `embedStyle` ternary for `cuppa` was simplified to hit the default `header{display:none!important;}` (removed the old `header .logo,header h1,header p` narrow override) — the whole Cuppa header now collapses when embedded, dropping Cuppa from 3 rows to 2 (bar + tabs). The controls stay reachable because the bar is outside the header; the opts gear stays hidden in embed via the shared `common` `.opts-btn{display:none}` injection. **This supersedes the v1.0.95 reasoning** (which kept Cuppa's header partly visible *specifically* so Sign in/Settings were reachable when embedded) — that constraint is gone now that those buttons moved to the always-visible bar. (2) **Rotating coffee banter** in the centre of the bar (`.lu-banter` + `startBanter()`, crossfades every 6.5s through `LU_BANTER`) — Jon asked for motivational banter but **no negative/individual callouts**, so the phrases are all positive and name nobody (kept separate from the pre-existing Wall-of-Shame "name & shame" feature, which was left untouched). (3) **Fun-stats "magic" pass**: the flat counter/fact tiles got icon chips, soft gradients, a spring hover-lift, scroll-in reveal, and **count-up** on the tallies (generalised `_countUp` to handle thousands separators so "~3,535" animates without mangling; `setupFunStatsIO`/`_revealFunEl` now also observe `.stats-counter-box`), plus accent ticks on the chart titles. Browser-verified standalone + embedded, light + dark.

**The core of the day:**
Two changes to **Cuppa**, edited in standalone `Cuppa/cuppa.html`, re-embedded via `embed.py`, browser-smoke-tested in both themes (light + dark) at `localhost:8899`. (1) **Prominent "Last updated" bar**: the timestamp used to live in an 11px `#hdr-status` badge in the header corner that nobody noticed. Replaced it with a full-width bar (`.lastupd-bar`) directly below `<header>` — a clock-in-circle icon, a mono `LAST UPDATED` eyebrow, and the time·date in the Fraunces display face on an `--accent-dim` ground. It sits outside `<header>` on purpose so it survives The Hub's embedded mode (which hides `header .logo/h1/p`). `renderLastUpdated()` now targets `#lastupd-value` instead of the removed badge; still driven by `state.updatedAt`, which is set on every admin save in `saveToFirebase()`, so Jon's manual data updates keep refreshing it automatically. Removed the dead `.lastupd-badge` CSS (definition + the 420px media-query hide). (2) **Editorial typography pass**: a prior "MODERN WEBSITE REDESIGN" block had loaded Fraunces and defined `--display:'Fraunces'` but never wired it into the UI (every title/number stayed on IBM Plex Sans; `--display` was only used in a canvas chart). Added a scoped block applying `var(--display)` to the true display surfaces — `header h1`, `.welcome-title`, `.stat-val`/`.stat-mini-val`/`.stats-counter-num` (with `tabular-nums`), `.table-title`/`.wos-title`, `.fun-stats h2`, the WoS total, and the empty states — while body text, tables, and labels stay on the sans. Jon liked this exact serif-display treatment from an artifact mockup and asked to bring it into Cuppa. Both changes verified rendering correctly against live Firebase data.

Previous session: 2026-07-06 (Ribbon audit / optimization pass)
Hub apps: 15.
Full code audit + optimization of **Ribbon** (protein structure renderer, 3Dmol.js) — edited standalone `Ribbon/ribbon.html`, re-embedded into The Hub via `embed.py`, committed + pushed to `main` (commit `8b4876b`). Correctness fixes: guarded fetch responses with a request-sequence token (`_reqSeq`) so a stale in-flight PDB load can no longer overwrite a newer one; fixed chain selection-dimming for blank chain IDs (was a truthiness bug — `selectedChain` can be `''`, now `selectedChain!==null`); `computeChains` seeds from polymer atoms (`{hetflag:false}`) only, so water/ligand-only chains don't consume a by-chain colour slot or become clickable; `recolorStructure` rebuilds the surface once (new `_surfaceTopologyStale()`) when a per-chain colour override lands on a unified spectrum/SS surface (previously the override hit only the cartoon underneath). Performance: chain select/deselect now takes the cheap recolour path instead of recomputing SES meshes (it was doing a full surface rebuild **twice** per click in surface mode); the label-callout `requestAnimationFrame` loop idles when no tags exist and restarts from `renderLabelTags`. Visual/UX: `.top-bar` wraps (`flex-wrap`) instead of `overflow-x:auto` scrolling the reset/export buttons off-screen in the ~760–950px band; theme toggle now shows sun/moon and persists to `localStorage`; HSL Light slider max tightened 170→150 so the default (100) sits at the true midpoint; non-selected chain surfaces now dim on selection (previously only the cartoon dimmed, so clicks read poorly in surface mode). Cleanup: removed dead `.viewer-toolbar` CSS and the hidden `#exportLabels` checkbox; merged the duplicated surface-material loop into `applySurfaceMaterials()` and the two bg-key mappings into `_setViewerBg()`; dropped the `state._lsShape`/`_lsBgMode` temp-on-state hack (`segHandler` now passes the clicked value straight to its callback). Enhancements: **mmCIF fallback** — on a `.pdb` 404, retry `.cif` (`addModel(text,'cif')`) so large/newer entries without a legacy PDB file load; persist + auto-restore the last-viewed structure on load instead of dropping to the empty state; new label tags stagger diagonally so they don't stack. Not yet browser-smoke-tested — the mmCIF-fallback parse on a large structure and the surface-mode selection dimming are the two behaviours worth eyes-on.

Previous session: 2026-07-01 (Lumina setup/output overhaul; v1.3.4)
Hub apps: 13. Version v1.3.4.
New app since the last logged session: **Lumina** (dose-response curve fitting, plate-grid input) was added and then taken through a full setup/output pass in one continuous day, all bundled together since these are all same-day iterations on one app rather than separate efforts:
- Setup modal restyled to match Echo's visual system throughout: section labels/grids, a drag-and-drop dropzone for the PHERAstar/GloMax reader file (replacing a bare file input), grid-based control-well picker (click wells directly on the plate instead of typing a range), auto-suggested next start well when adding successive compounds, and a one-click "Load test plate example" synthetic demo. Setup now opens automatically on load.
- Setup tabs renamed/reordered/iconified to Echo's Files/Assay/Analysis/Output convention, then further consolidated after review: Assay type + control-well marking moved into the Layout tab (a standalone Assay tab was too sparse on its own), and a new Output tab lets the user toggle which result columns (Top/Bottom/Hill/Dmax-Span-Emax/R²/Max SD/Flags) appear in the results table, Excel export, and TSV copy — Sample ID and the primary potency metric are always included.
- New: assay-aware DC50/IC50/EC50/Dmax/Span/Emax naming throughout (results table, chart axes, Excel headers) matching Echo's `_csvColMap` convention; Excel export now bundles Prism (%) and Prism (fraction) sheets, plus standalone "Copy results (TSV)" / "Copy Prism (%)" / "Copy Prism (fraction)" clipboard buttons; a "% of control / Raw signal" toggle on the Curves view.
- Plots/Curves/Plate were merged into one Visualization tab with an internal sub-nav, since Lumina's output is much smaller than Echo's and three separate top-level tabs left mostly-empty screens.
- Bug fixes: removed a browser confirm() popup on compound removal (now a silent toast); the Setup modal no longer visually jumps position when switching tabs (card height was auto-sizing per-tab while centered via flexbox — now fixed height).
Colors switched to the shared Nature palette across the whole session (accent f5c518→ffbf7b, accent2 0079b9→5e87c5, accent3 00c896→51c3ce), including previously-hardcoded chart/heatmap colors.
Also fixed: `.claude/settings.json`'s Stop hook pointed at a stale OneDrive path instead of this repo's `.claude/stop-hook.sh`, so the session log had gone stale silently for several versions (v1.2.3–v1.3.3 shipped with no log entries) — corrected to `$CLAUDE_PROJECT_DIR/.claude/stop-hook.sh` so future sessions auto-log again. Note: the hook script itself only writes a terse "Last session: DATE / Hub apps: N / Hub file size: X" stub (no narrative) — it will overwrite this detailed entry with that stub the next time it fires, same as it's always done; the auto-generated stub is only a starting point, not a replacement for a written summary like this one.

Previous bundle: v1.2.3 → v1.3.3 (2026-06-23 to 2026-06-30, undocumented at the time due to the stale-hook-path bug above — reconstructed from commit history)
Beacon (formerly NanoBRET Calculator) built out and renamed: Setup modal with Files/Assay Info/Plate Map sub-tabs, PHERAstar import, drag-paint dose-series Plate Map, auto-derived Endpoint/QC background-vs-DMSO pairing, Tracer Titration one-site-binding fit, Report tab, Excel export (Results/Plate Map/Assay Protocol/PHERAstar Protocol/Prism Copy), lite mode, embed-corruption fix (bundled SheetJS was breaking srcdoc embedding), scientific audit (Cheng-Prusoff Ki, tracer Kd fix, axis labels, error bars). Lab Designer: Excel-like keyboard navigation (v1.2.9), inline editing + square wells + sans-serif labels (v1.3.0), Illustrator-style color picker with Nature-palette swatches (v1.3.1). Suite-wide "Audit v1.3.2"/"Audit v1.3.3" passes: 5-app correctness/UX fixes, then full Nature palette rollout to Echo/Spectra/Beacon/Lab Designer/LabMate (shared swatches: #e36c69 salmon, #51c3ce teal, #5e87c5 blue, #ffbf7b orange, #816baf purple, #a56983 dusty pink, #c5c5c5 grey) plus a Spectra A260/A280 threshold fix (1.7→1.8, per literature), then LDI/Protein Tools got the same Nature-palette pass.

Previous bundle: v1.1.0 → v1.2.2 (Rounds 83–93, 2026-06-18 to 2026-06-22)
New app Iceberg/Cryostorage added (round 64) then seeded with Jon's real freezer/cryo-tank inventory + XLSX grid-template import/export (round 83). Echo: fixed a stray `</div>` collapsing the Plots tab's chart height (round 84). LabMate: unified 8 separate "grid→detail" navigations into one shared renderToolGrid() data-driven pattern, widened mobile/tablet breakpoint to 900px, fixed inconsistent back-button labels and a dead chip-render call (round 85); fixed an embedded-mode back-nav bug, widened mobile home grid to 2 columns, removed Pip mascot illustrations from always-visible UI (round 86); moved Cell Lines into its own sub-panel, replaced 9 mobile section icons with SVG (round 87). Suite-wide audit Phases A–C: mobile touch-target fixes across all apps + Hub logo SVG consistency (round 88); ~39 tab-navigation icons redesigned as line-pictograms across 9 apps (round 89); desktop density quick-fixes — max-width caps, sticky table headers, auto-fill grid fixes (Tier 1, round 90); desktop layout rework — Degradation Explorer empty-state CTA, Protein Tools chart divider, Spectra font bump (Tier 2, round 91); Guide-tab content icons added across 6 apps, Protein Tools Target Intel regrouped, Cuppa stat-card weight rebalanced (Tier 3, round 93). Firebase RTDB security fix (round 92): removed hardcoded legacy database secret from client JS in hub-shell.html and Cuppa, converted admin writes to Firebase Auth SDK, added version-controlled `database.rules.json`. Known accepted residual risk: the leaked legacy RTDB secret itself cannot be revoked (Firebase removed all console UI for this years ago) — see Firebase integration section above, don't re-flag without checking that note first.

Previous bundle: v1.0.11 → v1.0.68 (Rounds 45–82, 2026-06-11 to 2026-06-18)
